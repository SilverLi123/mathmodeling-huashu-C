# 华数杯 C 题：论文模型设计合同 v1.0

本稿按用户最终修订冻结模型路线，供后续 agent 实现和论文取证。已读取题面、附件1及全部6个工作簿，完成描述统计；本合同不包含尚未运行的预测、优化或消融结果。所有材料供学习研究参考。

## 0. 最终统一合同（2026-09-08）

政策标签采用 O/N/S/T/J，成本统一写为 F(政策)，避免政策名称与数值混用。

| 标签 | 定义 | 用途 |
|---|---|---|
| O | 附件原始运行结果 | 数据诊断；原始SOC终端不满足优化合同，不能当同约束最优对照 |
| N | 固定任务或固定负荷＋统一能源重新结算＋无储能 | 增量基线 |
| S | 同一固定任务或负荷＋优化储能 | 纯储能对照 |
| T | 优化任务＋无储能 | 任务调度对照 |
| J | 任务与储能联合优化 | 联合效果 |

实现与结果目录仍用 O/N/S/T/J 作为机器可读政策标签，但论文正文采用不易混淆的记号：Q3附件负荷族写作 \(N^A\)、\(S^A\)（A=Attachment load）；Q4共同约束比较合同直接写作 \(F_{00},F_{10},F_{01},F_{11}\)。`Q3_attachment_load` 的 N/S 使用附件固定AI负荷；`Q4_feasible_tasks` 的 N/S/T/J 使用同一个共同可行任务方案及同一实际任务集。两族不得混算增量。

成本变化为 F(S)−F(N)、F(T)−F(N)；正向节省为 F(N)−F(S)、F(N)−F(T)，联合节省为 F(N)−F(J)。F(O)−F(N)仅称“附件基准与统一口径重新结算的差异”，不得赋予单一机制解释；跨负荷族时另标明负荷也发生变化，不作纯机制归因。

全篇主指标为绝对界差 Δ=UB−LB（元）。需要归一化时采用 Δnorm=Δ/C_ref。C_ref为该基线族原始场景固定设施负荷按原始购电价计算的全量毛购电成本尺度；验证其严格为正后，在该族所有场景和算法对照中保持不变。不能用UB、当前场景成本或随算法变化的量作分母。跨族比较须另指定共同正尺度，否则不比较Δnorm。

每个界记录 `bound_type=full_problem/restricted_candidate/conditional_subproblem`、场景、目标、约束和候选范围。仅完整原问题有效LB可以形成全问题最优性差距；受限候选LB只用于其对应问题。求解器自报的MIP gap另列为诊断，不替代上述指标。

最终路线：Q1简单基准＋经回测选择的一个预测模型＋不可抢占基础调度；Q2 ε约束碳感知MILP，采用松弛—可行恢复—边际价格/瓶颈引导大邻域；Q3 LP-first / MILP-guaranteed；Q4由Q2调度重构AI负荷，固定新负荷重新求储能，再联合大邻域优化。禁止直接拼接Q3储能序列。原有数学约束仍全部有效。

核心贡献限定为：①带有效界验证的任务级松弛—恢复—大邻域求解策略；②无储能条件下“弃电—外送—购电”的三段边际成本机制，以及统一能源口径下的公平储能电量/容量/外送价值分解；③共同约束下四格交互及场景转折区间。第一项仅在等预算消融稳定支持后称“改进方法”，无证据不写“算法创新”。

## 1. 题目与输入

题面：2026年第七届华数杯 C题《面向算电协同的多目标调度优化研究》。输入位于 `C题 面向算电协同的多目标调度优化研究/`：题目PDF（2页）、附件1.docx、附件数据中的 workload_trace.xlsx、region_time_data.xlsx、GPU_information.xlsx、network_latency.xlsx、storage_information.xlsx、power_mapping.xlsx。输入只读。

官方发布页 https://m.saikr.com/contest/notice_detail/46038 明确比赛截止为2026年8月10日20:00；当前日期2026年9月8日，按赛后题目研讨处理，本次不涉及提交或论文导出。其他提交格式不在本次范围内，未核验。

## 2. 数据事实及其建模意义

- 50000个任务，TaskID唯一；到达小时0—2399。最后24小时到达538个任务。
- 6区域×2407小时=14442条电力记录，Hour—Region无重复，各区域完整覆盖0—2406。全部工作簿所读取矩形内无空值；数值异常尚不等于错误，不擅自修补。
- AI训练16559个，批量推理16717个，实时推理16724个。按GPU_Demand×EstimatedDuration_min/60计算GPU·小时，三类占比分别80.1376%、15.3183%、4.5441%。任务计数与实际资源压力明显不同。
- 持续时间10—399分钟。不能将每个任务一律向上取整为整小时运行；小时容量与功率按实际重叠时长计算。
- 训练、批量任务的LatestFinishHour全部为2406，没有短时业务截止要求；随意增加“训练6小时内完成”等硬约束会改题。
- 网络时延上限：实时20ms、批量80ms、训练150ms。候选目的区域必须逐任务查矩阵，不能默认批量任务全区域可达。
- 六区域AvailableRenewable_MW逐小时完全相同，范围500—1100MW，共13个不同幅值；不能从RegionE/RegionF标签推断原始数据存在光伏—风电互补。
- 14442条记录中只有RegionF第2400小时的给定设施负荷超过新能源：571.787867MW对540.19MW，缺口31.597867MW；其余记录均有新能源余量。
- 原始基准同时大量购电与弃电；能量平衡最大绝对残差约0.0002MW。原始基准储能终端D/E/F分别225.9765、217.2489、189.1754MWh，低于各自初值405、370、382.5MWh，不满足本题优化方案的终端要求。故不能把原始基准当成已通过全部优化约束的公平基线。
- IT_Load与NonAI+Baseline_AI重构残差不超过3.334e-7MW，基准IT及设施功率未超对应上限。此检查不代表任务GPU容量已通过。

**推论（待求解验证）：**只重分配新能源就可能大幅降低原始基准购电和碳排，储能额外减碳空间可能很小。必须拆开“附件原始结果与统一口径重新结算的差异”与“同基线族任务调度/储能/联合优化的增量收益”，前者不归因于单一机制。新能源充裕也不意味着所有调度都零碳或能满足GPU、IT等约束。

## 3. 四问的任务合同

| 问题 | 输入与决策 | 预期结论类型 | 推荐模型体系 | 验证与主要风险 |
|---|---|---|---|---|
| 一 | 任务历史做预测；实际任务决定区域与开工时刻；GPU、功率、时延参数限制调度 | 分区域分类型需求、留出预测精度、2376—2399任务清单及收尾、甘特图、利用率 | ①共享特征回归预测族；②不可抢占任务指派调度 | 时间切分无泄漏；前序任务占用正确；所有容量及截止通过 |
| 二 | 0—2399实际任务、0—2406电力参数；改变区域/开始时间及匹配的能源分配 | 成本—碳—网络延迟—新能源利用的可行权衡 | 同一混合整数调度族，采用ε约束法 | 统一功率映射；与重新结算基线比较；目标可能退化 |
| 三 | 固定Baseline_AI+NonAI及PUE；只优化储能与能源分配 | SOC/充放电策略及成本、碳、分区峰值、波动增量 | 分区域储能混合整数线性规划 | 固定负荷、初末SOC、能源来源、互斥；禁止接入第二问新负荷 |
| 四 | 实际任务、电力、储能参数；联合所有允许决策 | Pareto权衡、场景变化、联合协同增益 | 联合混合整数优化＋离线场景对照（不增加模型族） | 相同基线、场景、约束；分解算法需可行性修复和最优界对照 |

第一问预测与调度是两个独立任务，预测结果不替换任何一问的实际到达输入。第二问建议以无储能作为清晰的任务调度研究边界，并明确说明该选择；第四问再增加储能。这样比较时必须控制其他边界一致。

## 4. 统一调度骨架

任务i参数：到达a_i、最早开工e_i、GPU需求g_i、时长d_i=分钟/60、截止b_i=min(LatestFinishHour_i,2406)、类型k_i、来源o_i。区域r参数：可调度容量G_r、IT上限Imax_r、设施上限Lmax_r、PUE u_r。t=0,…,2406。

候选起点s先采用整数小时，这是建模离散化选择而非题面禁止分钟级开工。实时任务s=a_i；弹性任务s≥max(a_i,e_i)，s+d_i≤b_i。之后以更细起点网格检验离散化损失，仍保留真实持续时长。目的区域还须ℓ(o_i,r)≤MaxLatency_i。

x_irs∈{0,1}表示任务i在r于s开工，Σ_(r,s)x_irs=1。定义

α_ist=max(0,min(t+1,s+d_i)−max(t,s))（h）。

在Δt=1h下，U_rt=Σ_(i,s)g_i α_ist x_irs/Δt（等效GPU小时平均占用），A_rt=Σ_(i,s)p_(k_i)g_i α_ist x_irs/Δt（MW）。

约束：U_rt≤G_r，I_rt=N_rt+A_rt≤Imax_r，L_rt=u_r I_rt≤Lmax_r。N_rt为NonAI_IT_Load_MW。任务完整地连续占用同一区域，不可拆分、抢占或途中迁移。2406小时A_rt=0，但仍结算N_rt、电力和储能。小时GPU约束遵循附件的GPU-hour口径，不声称它自动保证任意分钟的瞬时GPU容量。

p_k来自唯一功率映射：训练0.16、批量0.10、实时0.08 MW/等效GPU。

第一问基本目标可按字典序最小化总等待、总网络延迟，再改善峰值利用率（线性上界变量）。所有硬约束先于软目标。不能把罚分当成违反实时SLA或任务截止的许可。绘制最后24小时甘特图时补充2400—2405收尾。处理2376之前仍在运行的任务：可先按同一基础政策调度前序任务形成占用快照；不能静默把2376视为空系统。

## 5. 第一问：先分清预测对象，再决定复杂度

统计同时给出到达GPU需求Σg_i、到达工作量Σg_i d_i、任务数与按来源到达即运行的逐时平均GPU占用；四者名称不得混用。推荐主要预测“区域×类型×小时到达GPU需求”，辅助工作量预测，更贴近调度压力。

先检验小时规律与自相关，不预设日周期、周周期。预测族采用共享18个区域—类型序列的回归模型：区域、类型、小时、预测起点前的滞后与滚动统计作为特征；弱信号时保留常数/收缩均值的退化形式，信号充分时使用正则化或梯度提升回归。不要同时堆叠多个独立预测族，使第一问超过两体系。

0—2351训练，2352—2375调参验证，锁定模型后0—2375重训，2376—2399最终测试。做24步直接预测或递归预测；不能预测后续测试小时时使用尚未观测的前一测试小时真实值。MAE、RMSE为主，汇总WAPE为辅（分母为0则不定义），不对大量零值组使用MAPE。因仅24小时官方验证窗，模型结构与特征选择可在训练段内部增加滚动回测，最终测试不参与选择。

不承诺预测精度；若没有稳定时间信号，诚实说明复杂模型没有收益。最后24小时调度以538个实际到达任务为对象，结合前序运行状态。

## 6. 能源结算与第二问

为避免购电转售、重复计新能源，定义逐时非负功率：新能源直接供负荷Rload、新能源充电Rch、新能源外送Rsell、弃电Rcurt；电网供负荷Gload、电网充电Gch；储能向负荷放电D。

Rload+Rch+Rsell+Rcurt=R（可用新能源）；Rload+Gload+D=L；C=Rch+Gch；B=Gload+Gch；P_sell=Rsell。

上述分流与附件总能量平衡一致；售电限定为当时新能源富余外送，不把电网购电或电池放电算作新能源售电。0≤B≤MaxGridImport；0≤P_sell≤min(MaxGridExport,SellLimit)。第二问无储能版本令C=D=Rch=Gch=0；能源分配仍需重算，不能复制UsedRenewable等基准结果。

总电费F=Σ_rt(π_rt B_rt−πsell_rt P_sell,rt)Δt；碳排E=Σ_rt c_rt B_rt Δt；新能源利用率ηR=Σ_rt(Rload+Rch+Rsell)Δt/Σ_rt R_rt Δt；平均网络时延H=Σ_irs ℓ(o_i,r)x_irs/n。碳排不能用设施负荷×碳强度，也不能扣除售电“抵消碳”。

推荐min F，约束E≤εE、H≤εH、ηR≥εR，扫描可行的ε值获得权衡。先求各单目标极值确定范围；如果碳已为0，相应碳约束失活，应如实报告，不人为制造Pareto曲线。新能源利用不等同于绿色用电比例，外送也算利用。高PUE可能反而提高名义新能源消纳，需补充单位GPU·小时设施能耗说明指标冲突。

迁移价值取决于新增任务的边际购电、边际碳排、占用新能源的机会成本、PUE、容量与时延。在充裕新能源下，低电价或低碳强度未必产生实际收益；占用本可出售的新能源有机会成本，利用本会被弃掉的新能源则不同。训练任务80.14%的资源占比使其成为重点，但不是无条件迁往西部。

### 6.1 无储能的三段边际成本结构

此节是Q2的解析解释与可等价降维，不替代公共能源分流检查模块。给定区域—小时设施负荷L、可用新能源R、外送上限X，在无储能、购电上限不绑定且 \(\pi^{buy}\ge\pi^{sell}\) 时，最优分流可解析为 \(B=(L-R)^+\)、\(P^{sell}=\min\{(R-L)^+,X\}\)。因此电费的局部边际成本为

\[
MC=\frac{\partial F}{\partial L}=\begin{cases}
0,&R-L>X\\
\pi^{sell},&0<R-L<X\\
\pi^{buy},&L>R.
\end{cases}
\]

相应地，边际碳排在前两段为0、在第三段为 \(c\)。分界点 \(R-L=X\) 与 \(L=R\) 不可微，采用子梯度或完整分流模型重算；若购电、外送或其他服务/系统约束绑定，以上只是局部解释，不能替代求解器的条件边际价格 \(\lambda_{rt}\)。对任务i，其在区域r增加的瞬时设施功率为 \(g_i p_{k_i}PUE_r\)，跨小时成本以该功率乘每小时重叠时长和对应MC累加。

在Q2实现中，可在逐时负荷给定后以该等价分段成本和碳函数消去部分能源变量，减少MILP规模；但必须用完整分流公共模块复算并验证等价，出现边界绑定或场景改变时回退完整表达。三段结构用于解释 \(\lambda_{rt}\) 的经济来源，不能将两者相加或互相替代。

## 7. 第三问：储能独立实验

固定L_rt=PUE_r×(Baseline_AI_IT_Load_rt+NonAI_IT_Load_rt)，不改变任务。SOC q_r,-1=InitialSOC_r，

q_rt=q_r,t−1+ηc_r C_rt Δt−D_rt Δt/ηd_r；MinSOC_r≤q_rt≤StorageCapacity_r；q_r,2406≥InitialSOC_r。

原问题含模式二元变量z_rt：0≤C_rt≤Cmax_r z_rt，0≤D_rt≤Dmax_r(1−z_rt)。先解取消模式变量的LP，检查每个区域、每个小时的互斥及全部其他约束。若LP已证最优且解满足原约束，则该解也是原MILP最优解（数值实现仅在公开容差内成立）。若有循环，在主成本最优面上次级最小化Σ(C+D)Δt，再验互斥；仍不满足则加入模式变量求MILP。用min(C,D)≤功率容差判定互斥，不能仅凭有量纲的乘积接近零。

次级求解保留主目标F≤F_LP*+τ_cost，记录τ_cost和次级解实际主成本；τ_cost>0时只能声明在该误差内最优。LP未证最优、次级失败或互斥失败均不能声称LP精确，回退MILP并报告有效界和时限。此流程逐场景执行；次级最小吞吐量不保证必然消除循环。

指标包括F、E，各区域峰值K_r=max_t(B_rt−P_sell,rt)，波动V_r=Σ_t≥1 |(B_rt−P_sell,rt)−(B_r,t−1−P_sell,r,t−1)|/2406。峰值取分区净购电；可另报告max(0,K_r)作为正购电峰值，不能混用。V是平均绝对爬坡，避免与标准差混称。绝对值和峰值用辅助变量线性化，在对应指标作为目标或上界约束时使用，并从实际方案重算指标。

以成本为主、碳/分区峰值/波动为约束，扫描阈值；无区域间物理电力流，在无系统耦合约束时可按6区域独立求解。比较①附件原始基准；②同负荷、重分配新能源、无储能；③同负荷、带储能优化。②到③才是干净的储能增量。基准终端SOC不合规应单列，不凭低碳改善百分比掩盖差异。

储能效率损耗也可能抬高题面新能源利用率，因此补报充放电吞吐量、损耗与终端净储能变化，并以次级最小吞吐量打破无意义循环。题面未给电池寿命成本，不能把外部假定的折旧系数混入“真实成本”；可在明确标注的敏感性实验中加入。

## 8. 第四问：统一模型、求解路线与场景

将第4节任务约束与第6—7节能源储能约束联合。F、E、H、ηR、K_r沿用统一口径；服务质量用分类型平均/P95等待s_i−a_i及慢化比(完成−到达)/d_i评价。可用线性总等待Q=Σ_i(s_i−a_i)/n作优化目标或阈值；不把等待小时与网络毫秒直接相加。

推荐仍以ε约束法形成少量有解释力的方案：低成本、低碳、低等待、低峰值及折中。满足相同硬约束之后才比较，不必把六指标凭空定权归为一个总分。

5万任务×6区域×约2400候选时刻可能达数亿二元变量。推荐先以“小实例精确MILP”检验数学口径，再用候选区域/时段筛选+大邻域重优化求全时域可行解；能源子问题给出边际成本供候选排序。候选受限模型只能声称该候选集内最优；逐步扩充候选并比较改进。若使用分解：固定任务后求能源MILP，任务阶段重优化，再回算全部约束；不能把含互斥整数的子问题直接套标准连续Benders对偶割。

短滚动窗口只是近似方案：弹性任务可延后至2406，故24小时窗口不能代表全局允许范围。若采用滚动优化，必须传递未完成任务、已开工连续占用、储能SOC及远期积压价值，做窗口敏感性和末端可行性检查。未获得下界与最优性差距时不称全局最优。

场景方向：
- 碳约束：以可行解的绝对碳预算扫描，若原始场景已零碳则报告预算不活跃；不可除以零基线构造百分比减排目标。
- 电价：原始分时价、各区保均值平价、均值受控的峰谷价差扩大；售电价关系同步检查，避免引入非预期套利。
- 新能源：原始数据保持不变；另建同比例降低、连续低出力、不同区域扰动等明确标注的假设场景。强度网格例如100%/75%/50%只是实验设计，不是估计的发生概率。按相同任务、固定非AI负荷比较。不能把新增光伏/风电曲线冒充附件事实。
- 本合同不启用随机/鲁棒模型；每个情景独立使用未来实际数据求解，只称离线场景对照。未来如另立版本，必须先明确非预见性和信息结构，再重新验收。

联合增益对照在同一任务与功率映射下设置：固定任务+无储能、只调任务、只调储能、联合。每一格都在同一个Q4比较合同下重新求解；若Q4含等待、峰值或碳等附加约束，Q2/Q3已有解最多作为热启动，不能直接拿来充当F10/F01。比较原始场景与压力场景，识别新能源不足到何种程度储能和碳约束开始发挥作用。

## 9. 假设、依赖、验证和停止边界

关键假设（2项）：①附件持续时长、任务功率系数作为确定性输入，符合题目实际输入要求；放宽需新增不确定性信息并重验期限。②仅按给定网络时延建模，不增加线路潮流/带宽/传输能耗，来自题面边界；改变边界需额外数据，本稿不做。

可放宽选择（3项）：①整数小时候选开工，细化网格复验；②第二问选择无储能边界，若改成固定储能必须保持约束与比较口径一致；③第四问压力场景幅度为设计值，通过幅度扫描复验，不声称概率分布真实。

后续可用pandas/openpyxl读数据、scikit-learn构建回归、Pyomo或直接求解器接口构建MILP（HiGHS/可用商业求解器）；实际安装与许可需在T00验证。建议图：区域×类型GPU工作量、预测对照、任务甘特、GPU利用率热图、成本碳权衡、储能SOC/功率、新能源分配、场景转折区间图。

验证顺序：输入完整性→任务唯一执行与时域→GPU/IT/设施/时延→能量守恒与来源→SOC初末及互斥→指标独立重算→相同输入的消融对照→候选集/时间网格/窗口敏感性。对浮点残差容限按输入精度与求解器容差明示，任务丢失/重复/越界不容忍。不可行时定位具体任务/区域/时刻，回退模型或增加候选，禁止用罚分隐藏。

模型路线已冻结；停止预算、容差和失败回退按任务执行指南执行。当前尚无优化性能结果或可行性证书，后续用真实实验决定论文保留哪些贡献，不再以讨论替代验证。

## 10. 理论来源与执行记录

已执行skill双引擎脚本：`hybrid_scholar.py --query 'carbon aware data center workload scheduling' --limit 3 --json`，退出码0；OpenAlex 15条、AnySearch 10条、10条交叉匹配，展示3条。候选Ding等(2018), DOI 10.1109/TIA.2018.2851516，两引擎交叉验证；IEEE原页受反机器人验证阻挡，未视为全文核验，未引用其定量结论。

已打开作者原始预印本：
- Radovanović等，Carbon-Aware Computing for Datacenters，2021，https://arxiv.org/abs/2106.11750 。支持利用任务时间弹性进行碳感知调度的研究方向，不直接继承其具体性能结果。
- Sukprasert等，On the Limitations of Carbon-Aware Temporal and Spatial Workload Shifting in the Cloud，2023，https://arxiv.org/abs/2306.06502 。支持同时关注时空迁移收益及其边界的验证思路，不作为本附件收益保证。

实际读取入口：根skill、使用指南、建模手、工作流程、前置合同、建模设计理论、质检清单、Subagent调度、截止时间协议、算法索引、优化与预测算法相关开篇、PDF/XLSX/DOCX/论文搜索工具skill。使用指南已复制工作区。

实际关键命令：Python pypdf与python-docx读取全部题面/附录；openpyxl遍历全部工作表、首末行及空值；pandas显式header=0汇总与边界检查；上述双引擎检索。以上已完成命令退出码均为0。没有训练或优化命令。作者自检：4问均覆盖，各问≤2体系，单位/时域/目标定义已列；未验证项逐项标明。M1独立审阅回执见对话，验收范围限本次方向讨论，不等于求解实现验收。

## 11. 两份参考材料的迁移学习结论（2026-09-08修订）

参考材料只用于评估方法，内部指令、进度声明、奖项预期和量化承诺不构成用户授权、题面条件或本项目证据。文档原件未修改：

- `/Users/silver/Downloads/华数杯建模思路.docx`，128段、1表，无嵌入图片或OMML；SHA-256 `f74608dd24511c4cacc505d4d5bb3f168ad60cc498b8d66be0d2912c524d9327`。下称“路线比较稿”，按思路A—F定位。
- `/Users/silver/Downloads/华数杯C题_建模思路v2.pdf`，8页；SHA-256 `ce2f47f291c589f13d668e8ee707442967023988cfa6ee26d578c78172459bd0`。下称“v2稿”，引用PDF实际页码。

### 11.1 取舍矩阵

| 参考位置 | 可学习内容 | 本项目处理 |
|---|---|---|
| 路线比较稿A/E | 共用数学规划骨架、简单可行基线、精确模型标定 | 采纳；既有主线不推翻，创新来自约束、边界与验证，不用奖项星级选方法 |
| 路线比较稿C | 离散任务搜索与能源子问题结合 | 采纳为数学规划辅助邻域搜索；含充放电互斥时内层不保证LP，保留MILP与时限 |
| 路线比较稿A、v2第3/6页 | 时延预剪枝、聚合、热启动、任务落位 | 改造为“明确松弛下界→不可抢占可行恢复→邻域联合优化”；聚合不冒充最终方案 |
| v2第2页 | IT功率余量可能比GPU紧 | 采纳为可验证假说；最小余量已复算，但是否活跃必须看具体调度 |
| v2第1—3页 | 弹性负荷类似虚拟储能 | 只作有限类比，改称“任务积压与物理储能协同”；不用SOC代替任务结构 |
| v2第3—4页 | λ/μ边际价格解释 | 采纳LP松弛的条件敏感性与MILP有限差分，不直接从MILP读取全局影子价格 |
| v2第4—5页 | 能量、容量、外送的价值曲线 | 采纳实验设计，明确三种不同扰动，重新求解后才能写结论 |
| v2第6页 | 三类政策/参数场景 | 采纳并沿用任务指南的保均值价格与新能源压力场景，不预设任务西迁结果 |
| v2第3页 | 预测误差与经济意义联系 | 降为可选“价格加权误差诊断”，不替换正式实际任务输入，不把诊断叫实际额外成本 |
| 路线比较稿B/D/F | 进化算法、强化学习、博弈 | 默认不引入；本题没有必须学习在线策略或内生定价的要求，避免范围膨胀 |
| v2第4/7页 | 放宽GPU/功率2%—3%以修复落位 | 拒绝；只能放宽聚合目标匹配误差，不能放宽硬容量、SLA、时限或SOC |

### 11.2 必须纠正，不能迁移的口径和结论

**（1）基准直用新能源不是外生上限。** v2第2/4页要求 `U≤基准UsedRenewable`，附件1及字段说明却明确该字段为“基准结果”。本题只有可用新能源输入及分配守恒，没有该上限。不能为了避免零购电解而改变可行域。直接反例：RegionA第0小时设施负荷499.149MW、可用新能源540.19MW，基准直用59.8979MW；无储能、无外送时直用499.149、弃电41.041、购电0满足能源平衡。这是单时段分配可行性反例，不是全题求解结果。

**（2）v2第4页两个能源等式不一致。** 它将U定义为新能源直用，却在总平衡右侧再次计入新能源充电/外送/弃电；同时G有时作为总购电，有时作为不含充电的购电，费用再加Cg。若G是总购电，正确总平衡应为 `G+R+D=L+C+S+Curtailment`，电费对G计一次；若G是电网直供，则 `G+U+D=L`，总购电为 `G+Cg`。本项目沿用§6显式分流，避免混用。

一个手算鉴别例：新能源100、负荷60、外送20、弃电20、无充放和购电。正确分流成立；v2印刷平衡左侧U=60、右侧L+S+Cu=100，不成立。仅凭文档不能断言其未提供的代码采用同样错误，但必须取得代码再复算。

**（3）v2第5页“已实证”数值不作为本题结果。** 当前工作区没有其声称的 `q3_{Region}.npy`、求解代码、日志和参数版本；且文中口径存在上述变化。因此22%左右节省、容量价值和V平台结论均标记为“参考作者的未复核结果”，不进入本项目摘要、图表或结果表。

**（4）不承诺全局最优、个位数MAPE、3%—5%误差或分钟级运行。** 全候选精确求解到最优才有相应证书；受限候选、聚合流和时间裁切各改变了比较对象。低误差和速度必须实际测得。成本为负或零时不能机械计算改善百分比。

**（5）实时任务开工固定，区域未必固定。** 原题只要求实时任务到达即开工且满足时延；固定来源/就近区域是基线政策，不是全问题硬约束。正式任务模型保留其可达区域选择；若为降维冻结，则所得解/界注明“固定实时区域条件下”。

**（6）其他数据陈述需收窄。** 本项目复算可调度GPU总量5205、全部任务工作量/2400约2091.9944 GPU（比例约40.19%），是主时域归一化工作量，并非每个时刻实际占用。IT余量最小值A/B/C/D/E/F为18.9800、18.213333、17.3720、260.9800、86.584667、66.583333MW。六区购电数值并非逐24小时完全相同，不能把分时标签重复当作100天价格值不变。任务真实时长10—399分钟，不能改称每任务1—7整小时。v2第2页“R>L占比0%”与其同句解释及实际数据矛盾，不采纳。

## 12. 增强路线一：聚合松弛下界与任务级可行上界

本节是§4任务MILP的求解与验证增强，不增加独立模型族。正式答案仍为逐TaskID、不可抢占的执行方案。优先先有可行初解，再增加下界分析，避免聚合落位失败阻断整题。

### 12.1 首选严格定义的连续松弛

小实例：将完整候选任务变量 `x_irs∈{0,1}` 放松为 `[0,1]`，储能模式变量也放松为 `[0,1]`；其余目标、参数和预算不变。原可行集合包含在松弛集合中，最小成本松弛的最优值为下界。只松弛受限候选模型不能得完整候选问题的下界。

大实例的可选紧凑流松弛：保留来源o、任务类型k、目的区域r和小时t，u_okrt≥0为该小时执行的弹性GPU·h，禁止不可达边，t仅0—2405。设A_okt为该来源类型在时刻t到达的总GPU·h，约束

`Σ_(r,τ≤h)u_okrτ ≤ Σ_(τ≤h)A_okτ`（所有h）；`Σ_(r,t)u_okrt=Σ_t A_okt`。

这保证累计不能执行尚未到达的工作量、期末结清；必须保留来源才能判断网络可达性。它允许拆分与抢占，故只作乐观松弛。平均功率为 `Σ_ok p_k u_okrt/Δt`，GPU平均占用为 `Σ_ok u_okrt/Δt`，和实时部分相加再执行§4全部功率限制。

若保留任务计权时延，增加每任务分数区域指派y_ir∈[0,1]，Σ_r y_ir=1，禁止不可达区域，且对弹性组 `Σ_t u_okrt=Σ_(i:o_i=o,k_i=k)g_i d_i y_ir`。网络平均时延按 `Σ_ir ℓ(o_i,r)y_ir/n`，不能拿GPU·h加权时延代替任务平均时延。累计约束可能仍偏松，但每个原始整数方案都有满足它的映射。

实时任务用y_ir乘其固定到达开始的完整占用曲线 `g_i α_i,a_i,t`，同一y贯穿时域；不要把实时任务改成可任意延后的流。固定所有实时地区只可得条件下界。

基础流松弛暂不表达逐任务等待预算时，显式删除该约束作为放松，在 `bounds.json` 记录；仍可给原成本最小化问题的较弱下界，但其价格不能解读为保留同样服务约束的价格。不能把工作流的“平均完成时刻”代替任务平均开工等待。固定负荷、PUE、购售电、碳预算、新能源利用等可线性表达的约束保持同口径；互斥做连续松弛时标明。

只有明确证明“每个原可行方案都能映射进该松弛，目标成本相同或更低”，才接受下界。任务聚合若丢失来源、到达、截止或改变功率映射，则先重写映射证明；没有证明只叫启发式引导模型。

### 12.2 可行恢复与界的报告

LP区域时段流只作为候选排序、水位参考和邻域选择；任务仍按完整时长落位。初解始终保留，落位冲突时允许偏离LP目标曲线、增加候选、交换尚未开工任务、局部MILP修复，禁止改变硬上限。实时任务不延后，已开工任务不迁移。

取有效下界LB、通过全量检查的成本UB，有 `LB≤F*≤UB`。若LP未证明最优，使用求解器有效的对偶可行下界而非任意LP可行目标。报告Δ=UB−LB（元），可附§0固定正尺度下的Δnorm=Δ/C_ref。无完整有效LB时全问题差距写“不适用”，负成本/近零成本仍保留绝对差。

以24/72小时代表实例检查精确模型与恢复策略，但必须写明其任务集合、运行截止、继承占用与SOC边界。小窗口重新设截止产生的是独立缩小问题，其误差不能称全时域真值差距。更严格做法固定窗口外任务/能源边界，再比较同一条件子问题的完整候选MILP。

## 13. 增强路线二：边际价格与瓶颈解释

采用正确能源结算模型的条件最优成本V_cost(L,εE)。在连续凸松弛且局部敏感性成立时，定义λ_rt为设施电量需求增加1MWh的边际成本（元/MWh）；碳约束乘子 `μ=−∂V_cost/∂εE≥0`（元/tCO2）。求解器对偶符号取决于等式方向，用小扰动复算确认，不直接照抄Pi正负号。

λ若来自已经包含碳预算的优化，其价格已反映碳约束的边际影响；**不能再加μ×碳强度重复计碳**。任务候选一阶能源评分为

`Score_i(r,s)=g_i p_(k_i) PUE_r Σ_t α_ist λ_rt`（元）。

这是固定线性化点下的筛选分数，不保证整条任务的真实增量成本；功率瓶颈、整数落位和储能联动使其非线性或不连续。正式接受方案仍重算能源子问题和全部约束。若额外加GPU资源稀缺价格，须有明确来源和单位；不得把已经折入λ的同一稀缺项再次添加。

MILP通常没有上述可直接解释的全局对偶价格；采用有限差分：同一问题略松/紧碳预算后重求成本，给区间减排成本 `(F(ε紧)−F(ε松))/(ε松−ε紧)`；标明两端可行解和求解gap。固定整数后的LP对偶只是该整数方案附近条件信息，不视为原MILP影子价格。若碳预算不活跃/碳排为0，μ或有限差分可能为0，保留真实退化结果。

瓶颈分析增加三张证据表：

1. 原始有效IT余量 `H_rt=min(Max_IT,Max_Facility/PUE)−NonAI`。
2. 最终方案GPU余量、IT余量、设施余量、购电/外送/SOC余量及各自达到容差边界的时段。
3. 对单资源上限作小幅放宽的成本变化，区分“低余量”与“有经济价值的约束”。

本附件设施上限与IT上限×PUE一致，二者属于同一物理瓶颈的冗余表达，单个对偶可能不唯一；不能把两个对偶相加解释为两项独立短缺。放宽有效IT容量作实验时同步调整对应设施上限，若只放宽一个而另一个仍等价限住，零收益不证明容量无价值。

理论依据：Gurobi官方Pi文档明确对偶适用于凸连续模型；Boyd与Vandenberghe《Convex Optimization》§5.6讨论扰动和敏感性。已核验入口：[Pi](https://docs.gurobi.com/projects/optimizer/en/current/reference/attributes/constraintlinear.html#pi)、[作者教材页](https://web.stanford.edu/~boyd/cvxbook/)。本节为基于该理论及本题结构设计的分析方法，不是已经算出的价格结果。

## 14. 增强路线三：任务积压、储能价值与协同收益

### 14.1 有限的“虚拟储能”解释

定义弹性工作积压 `Wrem_ok,t=Wrem_ok,t−1+A_okt−Σ_r u_okrt`（GPU·h），时段末包括已到达但未完成的剩余工作，初值0，2405末为0；正式任务方案的u由真实执行区间重构。可画积压曲线解释“任务延迟释放了当前负荷”。它不是能量SOC：执行地区PUE改变电量，GPU·h不是MWh；延期伴随等待成本，任务不能任意拆分，实时地区选择也不能被这个标量状态消除。无需新增虚构的“虚拟储能容量”或“充放效率”。

建议论文主线为“任务时空弹性与物理储能的协同调度”，可在解释段落引用虚拟储能类比，避免写成两个严格等价的电池模型。

### 14.2 储能三类边际价值，分开定义

令 `V_cost,r(t,q;K,X)` 为给定t之前已执行状态、从小时t到2406的固定负荷储能最小电费，t开始前SOC为q、容量上限K、外送上限X；终端要求固定为原InitialSOC，不随扰动变化，充放电功率/效率/其他输入不变。其他优化预算若存在必须固定，并说明采用哪个预算点。只有边界可行时才做以下扰动。

- **存量电量价值：** `vE_r,t(q;δ)=[V_cost,r(t,q;K,X)−V_cost,r(t,q+δ;K,X)]/δ`，元/MWh。δ可取0.1、1、5MWh检验局部稳定性；q+δ不能越上限。该实验是假设多给定存量电量的条件价值，不是实际方案免费注入电能；不得把扰动后的SOC轨迹当正式原问题结果。
- **容量价值：** 从同一个初态q0和终端下限出发，`vK_r(δK)=[V_cost,r(0,q0;K,X)−V_cost,r(0,q0;K+δK,X)]/δK`，元/MWh容量/本次结算期。扫描1.0、1.1、1.25、1.5倍基准容量，其他参数不变；下调容量必须仍容纳MinSOC和InitialSOC，不能随容量缩小赠送/收回初始电量而混淆效应。
- **外送通道价值：** `vX_r(δX)=[V_cost,r(0,q0;K,X)−V_cost,r(0,q0;K,X+δX)]/δX`，元/MW通道/本次结算期。需同时改变MaxGridExport和SellLimit，保持其他参数；ABC从0开放外送属于新的假设政策情景，不是原题优化权限。

禁止将vE、vK混称一个V(SOC,t)。边际值需要记录参数、单位、期长、差分方向和求解误差；若目标差小于两次求解不确定度，不宣称精确平台或拐点。扩大可行域下真实最优成本不应增加，发现增加先检查求解gap和配置一致性。Q3价值基于固定基准负荷，Q4负荷改变后不能直接当作正确的联合价值系数；可用作启发式初值或对照，最终应在联合解附近重新估计。

### 14.3 协同还是替代，用四格对照量化

同一场景、任务、基线调度和评价约束下，令F00为固定任务/无储能成本，F10为任务可调/无储能，F01为固定任务/有储能，F11为任务可调/有储能。四种政策的共同部分参数一致，并允许新灵活性“不使用”，保证可行域嵌套。

联合总收益 `Benefit=F00−F11`；储能在允许任务调度后的增量 `StorageAfterTask=F10−F11`；任务在允许储能后的增量 `TaskAfterStorage=F01−F11`；交互增益 `Interaction=F10+F01−F11−F00`。

令I=Interaction。解释符号前，四格必须使用同一任务集、服务质量约束、能源边界、目标和结算期，并全部通过可行性检查；每一格均在该统一合同下重新求解。各政策真正最优成本有有效界L_ab≤F*_ab≤U_ab时，计算 I_low=L10+L01−U11−U00，I_high=U10+U01−L11−L00。仅I_low>τ_I可判互补，仅I_high<−τ_I可判替代；否则写“交互效应在当前求解精度下无法判定符号”，不作统计显著性声明。τ_I仅由求解器绝对目标容差、独立重算误差及能量残差可换算的最大成本误差合成，预先固定并公开；它不是人为设定的经济显著性阈值。

缺少任一有效政策下界时可以报告已实现方案的交互量，但不将其符号解释为最优政策的互补/替代证据；受限候选界不足以认证全政策交互。某对照不可行则不计算交互量，另报不可行边界。启发式解不满足嵌套单调性时保留既有可行方案再重求。负电费仍可使用绝对收益。

容量/通道价值和交互量建议在原场景与一个新能源不足场景各做一次，再决定是否细扫。不要为得到明显收益而更改原始新能源直用口径。

## 15. 第一问可选经济误差诊断与新版验收要求

第一问正式预测保持18条区域—类型序列、既定训练切分和MAE/RMSE/WAPE，调度始终用实际任务。v2第3页“仅24×3预测”会遗漏区域层面结果，不作为本项目输出规模。

可选补充：用来源区域PUE将到达GPU·h预测误差换算为“假定来源执行的设施电量误差”，再乘仅用测试前信息估计并冻结的λ绝对值，汇总价格加权绝对误差（元）。它只是诊断评分，不是运行成本增加，λ为0时评分为0必须解释。预测GPU需求若没有时长信息不能直接按电量计费；必须使用另行定义的GPU·h预测或经验证的时长估计。

真正的预测—决策后悔值实验需要先定义相同实际任务流、预测驱动的预留/规划动作、到达后的可行修复及信息可见性，再比较总成本；没有这个机制不运行“把预测量当实际任务”的闭环。本项默认低优先级，不影响四问主线完成。

新版作者自检完成：两份参考均已完整读取；PDF第2/4页公式和上限通过页面渲染复核；附件字段和关键数据已复算；新增方法逐项定义单位、边界、失败回退、是否仅诊断。模型族数量不增加。旧稿M1曾因用量限制中断。本次v1.0已重新派发独立M1，回执见work/M1验收记录.md；没有覆盖当前快照的PASS前不能宣称通过。当前仍未训练、未优化、未复现参考作者的储能数值。

本轮新增文献检索调用双引擎 `hybrid_scholar.py --query 'data center virtual battery workload flexibility' --limit 3 --json`；脚本退出0但OpenAlex返回HTTP503，不能视为检索质量通过，结果不作已交叉核验引用。已用原始官方文档和作者教材页核验敏感性理论；虚拟储能仅保留为本题的有限类比，不以搜索失败的候选文献证明等价性。
