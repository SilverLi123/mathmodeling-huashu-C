# 论文初稿审阅意见

审阅日期：2026-09-09。对象：根目录Word/PDF、完整论文-LaTeX源码及其PDF、src代码、results实际输出和题面。此次只审阅与独立重算，未修改论文、模型代码或原始结果。结论：章节结构已经形成，但存在影响结果有效性的错误，应先修计算，再更新结论和排版。

## 1. 必须优先修正：Q2方案违反题目硬功率约束

题面第2页说明3明确要求GPU、IT功率、设施功率同时满足。`src/task_model.py:54`的迁移可行性判断只检查GPU；`src/checks.py`虽然读取了IT与设施上限，却没有执行对应检查，也没有从任务重新构造功率。因此结果中的“全部通过”不能证明完整可行。

按任务ID关联输入功率系数、真实时长，独立重构AI功率，再加非AI负荷并乘PUE，得到：

| Q2方案 | IT超限的区域—小时数 | 最大IT超限/MW |
|---|---:|---:|
| 20ms | 105 | 52.848 |
| 80ms | 765 | 82.092667 |
| 150ms | 889 | 72.320000 |

明确反例：150ms方案RegionB第2042小时，IT负荷482.320000MW，上限410MW；设施负荷651.132000MW，上限553.5MW。后一个数值与导出的`hourly_energy_q2_lb150.csv`直接一致。IT和设施上限在本数据中等价，不将两者重复计为两批时段。

影响：Q2的节省、迁移策略、碳权衡尚不能作为原问题可行结果；Q4使用此排程的F10及两阶段候选需要重新检查。不能靠修改“零违例”措辞解决，必须补约束和检查器并重跑。Q1此次独立IT检查未发现超限。

## 2. 必须重跑：预测存在目标与测试期泄漏

`src/forecast.py:33`的累计均值包括当前目标y_t；第39行滚动均值也包括y_t。回测及最终测试进一步用包含完整预测窗真实值的序列构造X_test，滞后1小时也会读入预测起点后实际值。这与正文“仅用过去信息、在固定起点预测未来24小时”不符。

只读反例：固定RegionD—AITraining的训练数据、已拟合模型和预测起点2376，只把2376—2399测试真值改为0，预测最大变化124.6634 GPU；首个预测由147.7742变成143.6794。预测依赖了不应已知的答案。

影响：WAPE=0.623、相对均值改善约5%的有效性未成立。应按固定起点的直接多步预测或严格递归预测重建特征、重做时间验证和模型选择。当前配置列出6个回测起点，正文写13个，也需统一。

## 3. Q4“任务在储能下边际价值为零”证据不足且日志冲突

`src/q4.py:55`的F10只比较固定基线与一份Q2排程；F11从“两阶段、F01、F10”三份候选中取最低成本。选中F01只能说明这些候选未超过它，不能证明整个联合可行域的任务增量为零。正文局部承认缺少下界，摘要、图注与总结却仍用“高度替代”“边际为零”“储能最优”等确定表述。

更直接的结果冲突：

- 四格表F01/F11：-459205361.431228元。
- `results/runs/base/q4/joint_lsn.json`的best_cost：-459295204.591966元，比四格表低89843.160737元。
- 该日志没有导出对应完整排程和能流供独立验收，因此不能立即将其视作有效新最优解，但必须解释或复算，不能忽略它再宣布无改进。

`src/joint_lsn.py`只把“区域发生改变”的任务纳入回退；当前Q2排程另有6091个任务区域没变但开工时间改变。因此“全回退=本地N”的日志注释不成立，即使跨区域任务全部回退也未必恢复原排程。这也解释了为什么所谓全回退成本可能与F01不同。

应导出并验收每个被比较候选；将可靠结果纳入四格。取得有效全问题界前，仅报告已验收策略的成本差与观察到的交互量，不认证最优政策的互补/替代。

## 4. 场景结论超出实际实验范围

`src/scenarios.py:36`每个场景复用原来的n_schedule和t_schedule，再结算能源；没有在新场景中重新搜索任务决策。

因此：

- [0.7,0.8]只能描述“既有两份排程的选优发生切换”的粗网格区间，不能证明任务弹性本身的价值归零。
- rho=0.4时两份固定排程无法无储能结算，不等于所有可调任务的无储能原问题都不可行。
- 不能以少量确定性场景重算声称已得到鲁棒最优策略。

应保持原共同基线作为主分析，报告其失效；另在每个场景真正重优化任务。若只有启发式未找到可行解，写“未找到”，不得写“已证明不可行”。场景特定修复基线单列，不能连成原四格曲线。

## 5. Q2碳预算实验目前只是有限方案筛选

`src/q2.py:180`的碳包络在N、T20、T80、T150四个既有结果中筛选，未在碳上限下重新优化任务。它可以作为候选方案集的包络，不能作为原调度问题的完整ε约束解或真实Pareto前沿；且这三个T当前存在功率违例。

Word及LaTeX模型检验部分的“epsilon<94时N可行”还遗漏下界：N本身排碳8.850562t，所以epsilon<8.850562时也不满足该方案的碳预算。应写精确数值区间，避免以四舍五入后的94、496作为严格可行判据。

相较冻结方案，当前也未落地有效连续松弛LB、任务级恢复证书、邻域MILP和等预算消融。实际方法是两轮价格评分贪心加有限候选储能求解，应如实命名，或补足原方法后重写论文。

## 6. 储能MILP回退约束写错

`src/energy_model.py:201`实际写成C-Cmax*z<=Cmax，即C<=Cmax*(1+z)，正确应为C-Cmax*z<=0。现写法在z=0时允许C>0，同时D<=Dmax，不能禁止同时充放；z=1时又允许C到2*Cmax。模式变量上界还应显式设为1。

当前基准运行记录为LP加次级求解，此错误不代表已导出的基准LP轨迹一定有循环，但“MILP-guaranteed”回退保证本身不成立，应先修复并用循环反例测试。

此外，次级求解后代码把返回cost改回首轮LP成本，能流却来自次级解。必须分别保存LP下界、次级实际成本和容差，不能配对为同一个解的成本与轨迹。

## 7. Q3应补题目要求的分区域峰值和波动

正文系统级峰值净购电-82.4/-413.5MW是跨区域汇总指标，不能代替题目要求的各区域峰值净购电。分区峰值与爬坡应单列，系统指标作为补充。

`src/q3.py:21`先对净购电数值排序再差分，破坏时间顺序。独立按小时差分，无储能D/E/F爬坡应为8.7484/11.4125/12.2144MW，原分区JSON却为0.05283/0.08171/0.10457MW。`metrics_q3_additional.json`的系统29.29/0.41采用另一汇总算法，不能用它证明分区JSON也正确。

措辞也应区分：当前模型是储能向本地负荷放电，释放当期新能源用于外送；“把储存电量直接外送”不是所实现的能源流。储能固定負荷子问题的最优性，不能推广成任务—储能联合全局最优性。

## 8. PDF、Word、源码及清单不是同一版本

| 内容 | 根目录完整论文.pdf | 当前Word/源码/结果 |
|---|---|---|
| Q2最低成本 | -4.384×10^8元 | -4.442×10^8元 |
| Q2节省 | 1.86×10^7元 | 2.44×10^7元 |
| Q2对应碳排 | 964t | 496t |
| PDF页数 | 23页 | LaTeX子目录PDF为24页 |

根PDF首页均值WAPE写0.627，Word及当前源码写0.657。Word原生公式XML中确有数值；普通段落文本提取会漏掉公式，不能据此误判Word丢失数字。

实际运行技能`equations.py verify-conversion 完整论文.docx`退出1：转换清单缺少规定字段，且DOCX哈希不一致。修复应从同一冻结正文、图表和结果重新导出两格式，再生成并验证清单，不能只把旧清单的hash手动换新。

根PDF第2页仅有关键词，已渲染确认。该版排版需要修复；但LaTeX子目录新版第2页已开始正文，不能把旧PDF问题泛化为所有版本。

## 9. 正文数学表达需要补齐

- Q4写成F00=min_{S∈空集} phi(Lfix)，空集上最小化不是“无任务决策”，应直接写固定负荷能源子问题值，或在单元素固定排程集合上定义。
- Q1把到达时刻a_i用作网络矩阵的来源区域下标，需改为来源o_i；加权等待与时延的权重/单位未交代，代码实际按区域优先搜索，也不等同于所述字典序求解。
- Q1/Q2正文约束未写全IT和设施上限；Q3正文只给总能量平衡不足以独立排除错误能源来源，应写出实际代码采用的分流等式。
- C*D=0本身是非线性互斥约束，若称MILP，应写正确模式二元线性化。
- SOC初态应标为第0小时之前的状态，避免把SOC_0同时用作第0小时末状态和InitialSOC。
- Q4服务质量与区域峰值的具体权衡尚不足。当前Q2平均等待从基线0.00172h增加至150ms方案5.29738h，不能只报告时延和节省而省略等待代价。

## 修复顺序

1. 补IT/设施硬约束、独立重构检查和错误状态传播；重跑Q2及受影响Q4。
2. 消除预测泄漏，重做回测、验证、测试。
3. 修储能MILP回退；统一导出实际能流成本和界；修分区爬坡。
4. 重建Q4候选证据，分场景重优化，补服务质量与碳预算实验。
5. 按实际证据收紧摘要、图注、结果与结论；最后统一导出Word/PDF并验收。

审阅验证范围：读取题面、正文源码、两版PDF与Word内容、求解代码和相关CSV/JSON；执行只读泄漏反例、任务功率独立重构、分区爬坡重算和版本检查，以上诊断命令退出0；DOCX转换验证退出1。未重跑全量优化、未逐篇重新核验参考文献、未完成Word逐页渲染，因此本记录不是W2通过回执。
