# 华数杯 C 题完整任务执行指南（Agent Implementation Plan）

**模型合同v1.0（2026-09-08）：** 路线冻结，执行前同时读取分析报告§0及§11—15。本轮统一O/N/S/T/J、固定正成本尺度、LP-first流程、四格交互界区间及场景转折区间；不再增加模型族。外部PDF中的新能源直用上限、硬容量放宽和未经复核储能结果明确不采用。

### 最终执行约定

- 所有RunID记录 `policy`、`baseline_family`、任务集哈希、固定调度哈希、服务约束、能源边界、场景和结算期。Q3附件负荷族N/S与Q4可行任务族N/S/T/J分开；论文正文将前者写作N^A/S^A，后者直接写F00/F10/F01/F11，O/N/S/T/J仅保留为机器可读标签。
- 成本变化与正向节省分列，纯储能节省为F(N)−F(S)。F(O)−F(N)只作原始与统一口径差异，不作单机制归因。
- `bounds.json`必须保存UB、LB、bound_type、Δ、C_ref、Δnorm及有效性依据。C_ref按分析报告§0在基线族原始场景计算后冻结；无有效全问题LB时全问题Δ/Δnorm留空并注明原因。
- Q4初解严格执行“Q2任务方案→重构AI负荷→固定新负荷重求储能→全量检查”，不得直接拼接Q3储能。若S也是联合可行方案，一并保留较优初解。
- 在T03手算验证中加入：负成本LB=-110、UB=-100时Δ=10；若C_ref=1000则Δnorm=0.01；LP次级成本不超过主目标容差；交互区间跨0时不得输出互补/替代结论。
- Q2消融比较普通/随机候选、价格引导、瓶颈引导、二者联合。使用相同总墙钟预算、共同初解、候选数量、邻域大小、停止规则和配对种子；松弛与候选构建耗时计入预算。报告UB、同一个完整有效LB对应Δ、可行率、耗时、种子范围。普通规则与随机规则不可混成一个不可复现方法，随机规则固定种子。
- 可行率=返回通过全量检查方案的次数/总尝试次数，同时报告“新邻域可行率”，避免保留初解使算法间全量可行率一律100%却掩盖恢复失败。仅联合引导跨种子和代表实例稳定改进时称“改进方法”，否则称“核心求解策略”。
- T09转折判据在扫描前登记：任务迁移比例变化、储能绝对节省变化及其数值误差；先粗扫再局部细扫，报告观察到的区间和网格分辨率。不把离散扫描结果宣称连续参数的唯一理论临界点。

当前冻结的是设计，不是实验结论；M1通过后由执行agent按T00—T12推进，P1通过才做全量Q1—Q4。后续若数据或可行性证据迫使改合同，先记录变更再复验，禁止静默改约束。

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. 使用复选框跟踪任务；默认由一个执行 agent 负责主线，仅在 M1/P1/P2/W1/W2 节点使用未参与产物编写的独立质检 agent。不得把本指南理解为已经完成实现或获得数值结论。

**Goal:** 根据本文件夹中的2026年第七届华数杯C题及实际附件，完成可复现的数据分析、四问模型求解、验证、科研图表和中文论文参考稿。

**Architecture:** 先建立唯一的数据契约、任务占用计算和能源核算，再让四问共享这些模块。第一问的预测与调度分开；第三问固定附件负荷；第四问合并任务与储能优化。所有图表和论文数字从通过独立检查的结果文件生成。

**Tech Stack:** 默认 Python；pandas、openpyxl、NumPy、scikit-learn、Pyomo＋HiGHS、matplotlib、python-docx。先检查实际环境，锁定实际可用版本；商业求解器仅在已有许可时采用。论文默认 Word；LaTeX 是用户明确启用后的附加分支。

**Spec:** 同目录 `题目分析报告.md`、`术语表格.md`、`使用指南.md`，以及 `C题 面向算电协同的多目标调度优化研究/` 内题面 PDF、附件1.docx和6个XLSX。题面与附件优先于本指南；本指南确定执行顺序，分析报告解释模型依据。

**当前状态：** 已完成读题、描述统计和建模方向讨论；此前 M1 PASS 仅限方向讨论稿。尚无预测训练、调度方案、优化结果或论文。下面所有复选框均为后续执行任务；出现“运行”指令不表示该命令已经执行。

## 0. 执行范围、路径和不可变规则

### 0.1 工作区

- 项目根目录：`/Users/silver/math-26华数C`。
- 权威技能根目录：`/Users/silver/.codex/skills/math-modeling-skill`，只读。
- 工作区也有 `math-modeling-skill/` 副本。不要混用两个版本的脚本；优先上述权威路径，若执行环境迁移导致其不存在，再核对本地副本的 VERSION 和脚本接口，并记录实际使用版本。
- 输入题目目录、6个工作簿和 `2026latex模板/` 均只读。新代码、结果、论文全部写入项目根目录下。
- 此前两个Markdown是讨论稿；执行 agent 可以在建模阶段将它们完善为可执行模型合同，变更后重新做 M1，不沿用旧回执。
- 本指南是用户明确要求新增的任务文档。内部日志不是正式论文的一部分。

### 0.2 必须保留的题目口径

1. 到达时域 `0…2399`；任务执行最迟结束于时点2406；`2400…2405`可收尾，`2406`没有AI任务占用，但有电力和储能结算。
2. 任务不可拆分、抢占或途中迁移；实时任务到达即开工。批量与训练任务的截止均为2406，不能自行改成短时截止。
3. GPU按实际执行区间与每小时的重叠时长折算；使用 `Available_GPU`，不是 `Total_GPU`。不能把分钟时长全部向上取整。
4. 网络按来源—执行区域的单向时延矩阵，所有类型均检查其 `MaxLatency_ms`；不增加潮流、带宽、迁移流量或传输费用模型。
5. 第一问预测严格按时间切分；任何一问的正式调度输入均为实际到达任务，不能用预测替换。
6. 第一、二、四问：AI功率必须由任务和 `power_mapping.xlsx` 重算；非AI负荷固定；设施负荷为总IT功率乘PUE。
7. 第三问：设施负荷固定为 `(Baseline_AI_IT_Load_MW+NonAI_IT_Load_MW)×PUE`，不得接第二问的负荷。
8. 碳排按总购电量乘碳强度累计，不能用净购电或总设施负荷替代；售电不能冲抵碳排。
9. 新能源利用包括直供、新能源充电和新能源外送，不重复计算电池放电，不把电网充电算作新能源。
10. 储能初态为运行第0小时前的 `InitialSOC_MWh`；时段末SOC递推，最终 `SOC(2406)≥InitialSOC`。
11. 输入中的基准购电、利用率、SOC等属于对照结果，不是优化方案必须复制的参数。
12. 每問最多两个独立模型体系；算法、候选集扩大、网格敏感性不单列为新模型。不得为了展示算法数量不断增加模型。
13. 数值结论来自实际运行。近似算法只能称可行解、候选集内解或启发式解；没有有效下界及差距证据，不写“全局最优”。

### 0.3 默认决策和需要停下的情况

可自行执行：本指南内的数据检查、模型细化、依赖安装到项目环境、代码编写、求解、作图、Word论文生成及明确标注的场景实验。不要每完成一个步骤就询问用户是否继续。

必须暂停受影响工作并说明证据：题面与附件存在无法消解的硬约束冲突；实际数据哈希变化；正式方案无可行解且扩大候选仍不能解决；必要引擎或官方格式无法取得。其他独立且有证据的工作可以继续。不得发送邮件、提交比赛作品或代替用户在外部发布。

当前是赛后题目研究，不虚构新的提交截止时间。用户若另给截止日期，再读取技能的截止时间协议并维护冻结时间和可导出版本。

## 1. 总体依赖、优先级与进度维护

执行链：

`T00环境与规则 → T01数据审计 → T02模型合同与M1 → T03公共计算与小实例P1 → T04预测 → T05基础调度 → T06碳感知调度 → T07固定负荷储能 → T08联合优化 → T09场景与图表/P2 → T10论文证据/W1 → T11论文导出/W2 → T12交付`

T04与T07在技术上可独立，但默认同一 agent 顺序完成。不要因可以并行就自行增加研究或实现子agent。独立质检按技能要求单独派发。

| 优先级 | 必须先保证的成果 | 允许后续改进 |
|---|---|---|
| P0 | 数据正确、模型口径正确、每问有通过检查的实际结果、全流程能复现 | 任何失败先修正，不以“赶论文”跳过 |
| P1 | 四问全部回答、公平对照、场景覆盖、至少12张合规候选图、完整Word稿 | 形成首个完整可打开版本后再增加实验 |
| P2 | 最优界更紧、候选集更广、多种子、更细时间网格、版式精修 | 不能破坏已经验收的结果快照 |

在 `work/执行记录.md` 记录每个任务的状态 `NOT_STARTED/RUNNING/PASS/FAIL/BLOCKED`、实际命令、退出码、输入/输出路径、耗时、失败证据和下一步。状态必须按真实执行更新。固定配置保存后，结果放在相应run目录，禁止混合不同配置下的表格和图。

首个基准配置用种子20260908；随机优化敏感性使用20260908、20260909、20260910。它们是复现实验选择，不是题面参数。计算时间先测小实例再估算，不承诺尚未测得的完成时长。

## 2. 拟建文件结构和公共接口

以下代码和结果文件均待执行 agent 创建；不要把未创建文件标记为现有成果。

```text
math-26华数C/
  任务执行指南.md                  本指南
  题目分析报告.md                  经M1确认的模型合同
  术语表格.md                      唯一符号与单位表
  使用指南.md
  requirements-lock.txt            实际依赖版本
  configs/base.json                全部基础参数、路径和数值容差
  configs/scenarios.json           场景定义与种子
  src/__init__.py
  src/io_data.py                   数据读取、键校验、规范化
  src/occupancy.py                 真实区间占用与功率映射
  src/metrics.py                   从输出独立计算指标
  src/checks.py                    独立约束检查，不读取优化器内部约束对象
  src/forecast.py                  预测特征、训练、时间回测
  src/task_model.py                共用任务指派约束与候选生成
  src/energy_model.py              能源分流和储能约束
  src/solver.py                    求解状态、界、时间预算、热启动记录
  src/relaxation.py                可映射的流/连续松弛、下界与LP价格
  src/sensitivity.py               碳预算、SOC、容量、外送的条件扰动
  src/q1.py                       基础调度
  src/q2.py                       无储能碳感知调度
  src/q3.py                       固定负荷储能
  src/q4.py                       联合优化及局部改进
  src/scenarios.py                 场景构造和对照矩阵
  src/figures.py                   从已审核结果导图
  src/paper.py                     论文构建及证据数字填充
  src/cli.py                      唯一命令行入口
  tests/test_occupancy.py
  tests/test_energy.py
  tests/test_forecast_split.py
  tests/test_checks.py
  tests/test_scenarios.py
  tests/test_small_optimum.py
  data/processed/                  从原始附件可重建的CSV缓存
  results/data_audit.json
  results/复现清单.json
  results/runs/base/               基础配置四问结果
  results/runs/scenarios/          各假设场景结果
  figures/                        SVG、300DPI PNG和灰度预览
  utils/plot_style.py              从技能复制，不另写替代风格工具
  work/                           内部模型选择表、日志、证据大纲和渲染检查
  work/checkpoints/                通过检查的独立快照
  完整论文.docx                   默认正式交付格式
```

公共数据契约：

- `tasks`：每TaskID一行，保留全部原始列，增加 `Duration_h`；不在原表写回。
- `region_hour`：每 `(Region,Hour)` 一行，完整14442行；原始输入列和基准结果列分组管理。
- `schedule.csv`：`RunID,Question,TaskID,SourceRegion,AssignedRegion,ArrivalHour,StartHour,FinishHour,Duration_h,GPU_Demand,TaskType,NetworkLatency_ms`。
- `hourly.csv`：`RunID,Question,Region,Hour,GPU_Used,AI_IT_MW,NonAI_IT_MW,Facility_MW,RenewableAvailable_MW,RenewableDirect_MW,RenewableCharge_MW,RenewableSell_MW,Curtailment_MW,GridToLoad_MW,GridCharge_MW,GridPurchase_MW,GridSell_MW,Charge_MW,Discharge_MW,SOC_MWh`。无储能场景相关功率为0；没有SOC决策时可填初值并说明，不借用基准末态。
- `metrics.json`：成本元、碳tCO2、新能源利用比例、平均网络ms、分类等待小时和P95、分类慢化比、各区净购电峰值MW、各区平均绝对爬坡MW、储能吞吐与损耗MWh、求解状态/上下界/差距/用时。
- `checks.json`：检查名称、PASS/FAIL、最大残差、最大违反量、对应TaskID/区域/小时、容差。必须从原始输入和导出方案重算。

建议公共函数签名（任务内可增加辅助函数，不悄悄改变下游接口）：

```python
def load_inputs(input_dir: str) -> dict: ...
def overlap_hours(start: float, duration: float, hour: int) -> float: ...
def reconstruct_load(tasks, schedule, region_hour, gpu, mapping): ...
def evaluate_solution(tasks, schedule, hourly, inputs) -> dict: ...
def audit_solution(tasks, schedule, hourly, inputs, config) -> dict: ...
def solve_task_energy(inputs, config, fixed_schedule=None, storage=False) -> dict: ...
```

签名中的省略号只表示接口，不是允许交付空函数。后续章节给出每个接口必须满足的行为和数值验收样例。

统一CLI由T03创建：`python -m src.cli COMMAND --config configs/base.json`。支持 `audit-data, smoke, forecast, q1, q2, q3, q4, scenarios, figures, validate, paper, reproduce`；命令成功退出0，数据/约束失败退出非零，不把异常吞掉。

## 3. T00：确认输入、工具链和论文格式

**输入：** 本指南、原始目录、技能根目录。**输出：** `configs/base.json`、`work/执行记录.md`、`work/规则与模板记录.md`、实际依赖锁文件。

- [ ] 读取本指南、两个讨论稿和使用指南；核对题面与全部附件，记录SHA-256。
- [ ] 读取建模手入口，执行阶段再读取编程手和论文手；遇到Excel、图表、DOCX、LaTeX时读取对应工具入口。不要一次加载无关算法资料。
- [ ] 先调用桌面 `load_workspace_dependencies` 定位运行时。当前可用Python路径是 `/Users/silver/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3`；迁移后重新发现，不假设此路径永远存在。
- [ ] 检查数据、优化、绘图依赖及HiGHS可调用性；缺少包时在项目环境安装并锁版本，不修改系统Python。记录商业求解器许可有无。
- [ ] 读取2026华数杯官方规则和论文模板，记录来源URL、文件哈希、格式、页数、摘要、匿名要求、引用及AI使用说明。已有发布页线索：`https://m.saikr.com/contest/notice_detail/46038`；提交截止2026-08-10已过去，本次是研究参考稿。
- [ ] 检查现有 `2026latex模板/`：其规范文件明确是“全国大学生数学建模竞赛论文格式规范”，主文件含国赛30页等注释、占位标题“论文222标题”，模型章节只引入3问。**它不是已核验的华数杯模板，现有论文.pdf也不是本题结果。** 不直接使用其规则；用户启用LaTeX后复制并适配，增加第四问，删除模板示例。
- [ ] 默认最终生成中文 `完整论文.docx`。模板目录存在本身不等于用户要求LaTeX。若后续用户明确要求LaTeX，则同时交付Word、LaTeX源项目和实际编译PDF。
- [ ] 早期只做导出环境诊断；P1通过后可用真实小实例做一页公式/图表导出冒烟检查，不能把小实例包装成完整论文。

可直接运行的已有技能命令（使用实际发现的Python执行）：

```bash
python /Users/silver/.codex/skills/math-modeling-skill/references/roles/编程手/scripts/check_env.py --features data visualization optimization
python /Users/silver/.codex/skills/math-modeling-skill/tools/docx/scripts/check_env.py
python /Users/silver/.codex/skills/math-modeling-skill/tools/docx/scripts/self_check.py
```

**验收：** 输入版本和环境可追踪；求解器能解一个2变量小问题；Word依赖诊断有真实输出；规则未取得则标明格式受限，不把国赛规则冒充华数杯规则。允许继续数据与模型研究。

## 4. T01：数据处理与审计

**文件：** `src/io_data.py`、`results/data_audit.json`、`data/processed/*.csv`。**依赖：** T00。

- [ ] 先读取工作表原始首行确认表头，再显式设置 `header=0`。优先调用技能 `tools/xlsx/scripts/read_rows.py` 的 `read_excel_rows(..., header=True, expected_rows=...)` 核对主表行数。
- [ ] 验证主表规模：任务50000，电力14442，GPU6，储能6，网络36，功率映射3；记录所有辅助工作表，不把字段说明当数据。
- [ ] 检查唯一键、空值、类型、正GPU/正时长、任务区域/类型枚举、到达和截止、最早开工、非抢占标记。任务按到达排序但保留TaskID，不能依原始行顺序当时间。
- [ ] 验证六区域每个0—2406小时均存在；计算图使用任务主时域和电力全时域的不同长度。
- [ ] 不对“新能源同序列”等结构性异常做插值、去重或重造；原数据不变，异常进入报告。
- [ ] 重算三类任务数和GPU·小时、区域分布、每小时到达需求、工作量、即时运行占用；对无到达的区域—类型—小时补0，不能把补0当缺失数据填充。
- [ ] 核验能源平衡、IT重构、SOC基准终端，并区分“输入合法性失败”和“基准方案违反新优化约束”。

回归锚点（输入哈希不变时应重现）：

| 指标 | 预期 |
|---|---|
| 最后24小时实际任务 | 538 |
| 训练/批量/实时数量 | 16559 / 16717 / 16724 |
| 训练GPU·小时占比 | 0.801376158189607，浮点比较 |
| 六区域新能源逐小时是否相同 | True |
| 基准新能源小于设施负荷记录数 | 1 |
| 唯一缺口 | RegionF，2400小时，31.597867MW |
| 基准能量平衡最大残差 | 约0.0002MW，保留原始精度解释 |
| D/E/F基准终端SOC | 225.9765 / 217.2489 / 189.1754MWh，低于初值 |

输入哈希锚点：

```text
workload_trace.xlsx 006c0802f4a5a94a94eb7c54f8333091ac860c7d35c9c2c9ad04c68d4f0165ba
region_time_data.xlsx 0386a1f6ae825fc4e9797528545090863817b73154050f3ba7a1068e456d55ba
GPU_information.xlsx 68f978ff242da0bab54259283f647c7552693acf6db5e2212ff4e10f6a5d5d2f
network_latency.xlsx f7e16fc33d8c188b0889e358497e0d96451e04c77ce765a667a50488f02ac157
storage_information.xlsx ff5384eb6e955079766b22432e630dfdec9f347f11cd334d950716b21692b3e4
power_mapping.xlsx a5bea2b708cc4e5586e59bfd832b8dde7b629d212fcd8c3a0f132c03163ae39e
```

**验收：** 行数/键/字段检查通过；锚点可重现；没有偷偷删行、改单位、改新能源。锚点失败先查文件版本和读取方式，不直接改预期值。

## 5. T02：完成模型选择、算法选择与M1

**修改：** `题目分析报告.md`、`术语表格.md`。**内部记录：** `work/模型选择记录.md`。**依赖：** T01。

为每问填写“要回答的结论→可用字段→数学模型→算法→验证→失败回退”六列；把讨论稿中模糊的可选项收敛到以下默认路线，有实际证据才换方案。

| 问题 | 首选模型 | 首选算法 | 选择依据与回退 |
|---|---|---|---|
| Q1预测 | 共享18条区域—类型序列的回归预测族 | 先收缩均值/正则化回归，训练段回测证明非线性有效再选梯度提升 | 弱周期不强行深度学习；最终只保留一个选定预测模型，简单均值作为性能基准 |
| Q1调度 | 不可抢占区域—起点指派 | 全时域可行构造＋最后24小时局部MILP，按等待/时延字典序 | 精确表达重叠占用；构造失败允许重排未开工任务，不丢弃任务 |
| Q2 | 无储能碳感知任务—能源MILP | ε约束＋松弛价格引导＋候选集局部重优化 | 任务解给上界、完整有效松弛给下界；价格仅作条件解释 |
| Q3 | 固定负荷储能LP-first / MILP-guaranteed | LP→最优面次级最小吞吐量→必要时MILP；无系统耦合时分区求解 | 每场景验证互斥；主目标误差与有效界必须记录 |
| Q4 | 联合任务—储能—能源MILP | 可行热启动＋受限邻域联合求解＋ε约束＋四格交互分析 | 同模型扩展；区分协同和收益替代，随机/鲁棒默认不启用 |

- [ ] 定义整数小时开工为基础离散化，保留真实分钟时长；后续以30分钟网格在代表实例检验损失。不擅自声称题面要求整数开工。
- [ ] 写全任务唯一指派、释放/截止、实时即开工、延迟筛选、GPU/IT/设施功率约束；2406 AI负荷严格0。
- [ ] 写全新能源分流和购售电边界；默认只有当时新能源可外送，电网电和储能放电不直接转售；检查实际买价≥卖价，场景变价保持合理关系。
- [ ] 写全SOC、功率上限、效率、充放电互斥与终端状态；检查器独立验证。
- [ ] 定义成本、碳排、利用率、任务平均时延、分类等待、慢化比、分区域净购电峰值和平均绝对爬坡。平均时延按任务计权，不偷偷改成GPU计权；若另报告GPU计权必须更名。
- [ ] 冻结Q1前序状态政策：按同一基础调度处理2376之前全部任务；2376时保存正在执行占用和未开工积压，最后24小时求解不能无视积压。
- [ ] 保留完整可行初解的全部指派作为候选，后续局部优化冻结外部任务的实际占用。禁止把“先只运行实时任务”作为不可更改事实导致弹性任务无空间。
- [ ] 对低碳、消纳、峰值等指标先求单目标范围。ε阈值来自可行范围，零碳时碳预算失活，不能生成虚假差异。
- [ ] 文献按技能双引擎检索，再打开作者/DOI页面核验。优先已有两份来源：`https://arxiv.org/abs/2106.11750`、`https://arxiv.org/abs/2306.06502`；补齐不可抢占调度、储能和ε约束所需依据。未核验文献不进入最终参考文献。
- [ ] 列出关键假设与可放宽假设、数据依据、求解器、误差指标和边界测试，完成建模手清单。
- [ ] 派发独立M1，输入题目、附件、更新后的两个Markdown、文献和自检记录；M1 PASS后才进入T03实现。旧讨论稿PASS不能替代本次可执行合同验收。

**默认优化预算：** 小实例完整候选MILP每次最多120秒；代表实例600秒；全量首个方案每次最多1800秒。相对MIP gap目标1e-3是计算设置，不是合格保证；超时有可行解则保存解与差距，未找到可行解则FAIL并回退。成本可能为0或负，必须同时报告绝对界差；有效全局下界缺失则gap写“不适用”。

**规模控制：** 候选时刻每任务先32个，验证64、128扩充，来自整个允许时间域的分段低成本时点、最早/最迟时点及初解时点，不只取未来24小时。每次建立模型前估算变量/非零系数/内存；超出实际可用资源就缩小联合邻域，不能截断任务总量。邻域先200、500、1000个任务测时，选择能在预算内求得可行改进的最大规模。

## 6. T03：公共计算、独立检查器和P1小闭环

**文件：** `occupancy.py,metrics.py,checks.py,task_model.py,energy_model.py,solver.py,cli.py`及对应测试。**依赖：** M1 PASS。

- [ ] 先编写下列手算测试，确认未实现时失败；再实现最小公共函数并通过。只写能发现真实口径错误的测试，不机械测试每个getter。
- [ ] 对任务区间使用半开区间 `[StartHour,FinishHour)`，逐小时重叠公式为 `max(0,min(t+1,finish)-max(t,start))`。
- [ ] 用统一映射把GPU·小时折成小时平均MW，固定非AI＋AI后乘PUE；不从基准利用率反推任务功率。
- [ ] 导出方案后，检查器仅使用原附件和导出行重算占用、能量、SOC和指标，不使用优化器变量值以外的内部表达式。
- [ ] 为缺TaskID、重复TaskID、跨2406、非法区域、超时延、超容量、伪造新能源充电、终端SOC不足分别做反例，保证检查器返回FAIL及具体记录。
- [ ] 小实例用6区域的真实参数和少量真实任务，裁切到短时域时显式命名为“小实例”，单独定义其终端，不冒充题目全时域。
- [ ] 在结构等价手算实例上，枚举全部可选指派，与MILP最优值比较；没有枚举/精确参考时不能用“和另一个启发式接近”证明正确。
- [ ] `smoke`贯通读取→预测接口的训练样例→任务指派→储能能源求解→CSV/JSON→独立检查；小规模即可，不要求正式图表。
- [ ] 保存小实例命令、输出、上下界和容差；派发独立P1。P1通过之前不开始全量优化、场景扫描和正式出图。

必须通过的数值样例：

```python
from math import isclose
from src.occupancy import overlap_hours

assert isclose(overlap_hours(10.0, 1.5, 10), 1.0)
assert isclose(overlap_hours(10.0, 1.5, 11), 0.5)
assert isclose(overlap_hours(10.0, 1.5, 12), 0.0)
assert isclose(overlap_hours(2405.0, 1.0, 2406), 0.0)
```

其余测试明确按以下输入/预期实现：

| 测试 | 输入 | 预期 |
|---|---|---|
| 功率 | 10 GPU训练，1.5h，10时开工 | 10时1.6MW、11时0.8MW，AI能量2.4MWh |
| SOC | 初态100，充10、ηc=0.9，再放9、ηd=0.9 | 两小时末109、99MWh |
| 无储能能源 | 负荷60、新能源100、外送上限20 | 直供60、售电20、弃电20、购电0 |
| 碳与成本 | 购电10MWh、碳强度0.5、买价400，无售电 | 碳5tCO2、成本4000元 |
| 时限 | 2405时开工、持续90分钟 | FAIL，超过2406 |
| 基准新能源 | 购电给电池充10MWh | 不能使新能源利用分子增加10 |
| 基线零成本 | 成本从0变为负值 | 输出绝对差和收益，百分比改善不定义 |

数值容差初值写入配置：时间1e-8h、功率/SOC检查1e-5对应单位、二元完整性1e-6；输入基准平衡额外用1e-3MW兼容小数舍入。前者不能自动放宽到后者；若求解器尺度需要调整，先缩放变量并记录，再经复验改变容差。TaskID缺失/重复等离散错误不容忍。

未来命令：

```bash
python -m pytest tests/test_occupancy.py tests/test_energy.py tests/test_checks.py tests/test_small_optimum.py -q
python -m src.cli smoke --config configs/base.json
```

## 7. T04：第一问的需求统计与预测

**文件：** `forecast.py`、`tests/test_forecast_split.py`；输出 `results/runs/base/q1/forecast_*.csv`。**依赖：** P1。

- [ ] 生成18条小时序列：主目标到达GPU需求；辅助目标到达GPU·小时工作量；分别定义名称单位。
- [ ] 只在0—2351内评估自相关、小时效应、零值率与训练段滚动回测。建议回测起点2208、2232、2256、2280、2304、2328，每次预测24小时。
- [ ] 采用24步直接预测，共享区域/类型和步长特征；每一条训练样本的滞后/滚动统计都只能用预测起点之前信息。候选滞后1、24、168小时，滚动窗口24、168小时；小时是相对时域特征，不虚构实际日期/节假日。
- [ ] 均值基准采用各区域—类型训练历史均值；回归用训练折内预处理。非线性候选只在训练段回测改善稳定时保留，不在最终测试上挑模型。
- [ ] 使用2352—2375验证确定最终配置；锁定后用0—2375重训，预测2376—2399。特征需要超出训练范围的真实值时，改为直接预测或递归预测，不能泄漏。
- [ ] 报告MAE/RMSE、按区域与类型误差、汇总WAPE；零分母返回缺省并说明，不用epsilon偷偷掩盖不可定义指标。负预测值的截断必须作为锁定模型的一部分，在验证与测试保持一致。
- [ ] 若复杂模型不胜均值，选择简单模型并解释噪声水平；不为“漂亮准确率”改变测试切分。

**验收：** 18×24条最终预测完整，每条含真实值、预测值、预测起点、模型ID；修改测试段真实值不改变训练特征和已锁定预测；测试结果不影响选择日志。运行 `python -m src.cli forecast --config configs/base.json`。

## 8. T05：第一问基础调度

**文件：** `q1.py`，输出 `results/runs/base/q1/`。**依赖：** P1、冻结的Q1模型合同。

- [ ] 对0—2399所有任务构造容量可行的基础方案。先按实时刚性、截止紧迫度与任务资源需求排序；搜索满足时延的区域与连续执行起点。排序只是启发式，不把失败等同于原问题不可行。
- [ ] 实时任务始终固定开工，不允许靠延后它们修复容量。构造受阻时重新选择区域或重排尚未开始的弹性任务，并保留失败证据。
- [ ] 建立2376时点状态：前序正在执行任务、所有未开工积压、各小时剩余GPU和功率容量。旧任务不能在最后24小时展示中消失。
- [ ] 对最后24小时538个到达任务及必要积压任务做局部优化，外部已执行安排冻结；可选目标按总等待、总网络时延、峰值占用依次求解，上一层目标值作为下一层约束保留。
- [ ] 导出全量调度与 `schedule_last24.csv`，全量方案用于核对前序占用；后者准确包含538个目标任务。
- [ ] 输出六区逐小时GPU利用率，分母 `Available_GPU`；甘特图明确显示2376—2399及2400—2405收尾。538行过密可分区分页，另附完整任务CSV，不能抽样图冒充全部任务安排。
- [ ] 在Q1任务侧检查通过后，执行统一无储能能源重结算，检查购电/外送上限、能量守恒及完整结算期。通过才冻结为Q4共同固定任务基线；失败时记录违反区域—小时并进行可追溯的最小任务调整或能源修复，另存修复前后RunID，禁止静默替换。

**验收：** 全量50000任务一次且仅一次；目标538个完整；GPU/IT/设施/时延/时限全通过；2406 AI负荷0；甘特图完成时刻与CSV一致。运行 `python -m src.cli q1 --config configs/base.json`。

## 9. T06：第二问碳感知调度与基线

**文件：** `q2.py`，输出 `results/runs/base/q2/`。**依赖：** T05的可行热启动。

- [ ] 先实现无储能能源重分配，并在固定可行任务调度下计算购售电、弃电、碳排。
- [ ] 实现并测试无储能解析分段函数：在购电上限不绑定且买价≥卖价时，给定L/R/X验证B=(L−R)^+、P_sell=min((R−L)^+,X)，以及弃电/外送/购电三段边际成本和碳排。Q2可在满足前提时使用其等价紧凑分段表达降维；完整能源分流模块仍作为统一复算和边界回退实现。记录每个时段是否满足解析前提。
- [ ] 建立两个不同名称的对照：附件原始基准 `original_baseline`；同一可行任务方案重新结算的 `feasible_fixed_no_storage`。前者用于诊断，后者用于优化增量比较。
- [ ] 按实际任务与逐时参数优化区域/开工，候选覆盖完整允许时域并始终包含初解。每次接受改进前检查所有硬约束。
- [ ] 先分别探索成本、碳、时延、利用率范围，再扫描碳/时延/利用率ε约束。首批最多5个有差别的碳预算点；退化时报告相同解，不重复制造曲线。
- [ ] 记录任务迁移比例、来源—目的迁移矩阵、分类型等待及边际成本解释。东部不具备外送能力时，承接任务可能利用原本弃电；不能只按电价从低到高分配。
- [ ] 报告每个受限模型的候选规模、时间、最好可行目标、有效下界、是否全局候选。完整问题的下界必须由完整问题的有效松弛获得，不能用受限候选模型下界冒充。

**验收：** 每方案全量检查通过；统一功率映射；基线和优化的比较时域都含收尾与2406结算；改进百分比分母接近0或为负时改报绝对变化。运行 `python -m src.cli q2 --config configs/base.json`。

## 10. T07：第三问固定负荷储能

**文件：** `q3.py`，输出 `results/runs/base/q3/`。**依赖：** 公共能源模块，不依赖Q2新负荷。

- [ ] 从附件重构固定设施负荷，逐元素断言与 `PUE×(Baseline_AI+NonAI)` 一致；Q3函数不接受任务重调度输入。
- [ ] 使用 `Q3_attachment_load` 族的O/N/S；固定附件负荷，原始SOC终端不合规单列。储能节省为F(N)−F(S)，成本变化为F(S)−F(N)。不得和Q4负荷族混算。
- [ ] 按真实SOC初态建立0—2406递推，充放电互斥，外送与购电上限；所有时段均进入成本/碳结算。
- [ ] 成本为主，分别分析碳、分区域净购电峰值与爬坡约束；全系统碳预算会耦合区域，不能继续声称6个子问题完全独立。
- [ ] 从优化结果重新计算峰值与波动，避免仅读取未被目标压紧的辅助变量。波动定义为相邻净购电变化绝对值的均值；另有标准差时单列。
- [ ] 先解LP并检查全时段min(C,D)≤1e-5 MW；有循环时在F≤F_LP*+τ_cost面次级最小吞吐量，再检查；仍有循环或LP未证最优则回退MILP。次级目标不保证必然消除循环。记录主目标证书、τ_cost、实际主成本、互斥残差和求解分支；仅在相应容差内声称最优。
- [ ] 补报损耗、吞吐量和终端净储能。电池折旧只可作为有标注的敏感性参数，基础电费不含凭空估计费用。
- [ ] 若储能增量减碳为零或很小，保留真实结果，讨论其在新能源充裕条件下的边界。

**验收：** 固定负荷不变；SOC/互斥/功率/能量/购售电全通过；主结论分别报告成本变化F(S)−F(N)与正向节省F(N)−F(S)，而非全部归因于相对原始基准的收益。运行 `python -m src.cli q3 --config configs/base.json`。

## 11. T08：第四问联合优化

**文件：** `q4.py`，输出 `results/runs/base/q4/`。**依赖：** T06、T07已验证模块。

- [ ] 合并任务决策与能源储能决策，目标为成本，碳/网络时延/等待/利用率/区域峰值用ε约束或分层优化，避免未经依据的六目标权重求和。
- [ ] 用Q2可行调度固定任务后求储能，得到联合模型的可行热启动；若加新ε约束使其不可行，先寻找符合新预算的可行点。
- [ ] 四格F00/F10/F01/F11必须在同一个Q4比较合同（任务集、服务约束、能源边界、结算期、目标）下分别重新求解。此前Q2/Q3结果只能作热启动或代码验证；若Q4新增等待、峰值或碳约束，不得直接复用为F10/F01。
- [ ] 联合邻域同时放开一批任务指派和受影响的能源储能变量。冻结其余任务但计入占用；SOC如果只优化局部时段，必须固定边界状态保证与全时域拼接一致。
- [ ] 实施“选择高边际成本/容量瓶颈任务→扩展候选→联合重求→独立检查→接受实际改进”的循环。固定储能只重调任务的交替算法可作为热启动，不足以证明联合最优。
- [ ] 初始预算至多20轮或60分钟，连续5轮无超过数值容差的实际改善则停止并记录；保留最好可行解。此处是计算预算，可据测时调整，不是理论收敛证明。
- [ ] 记录成本、碳、等待、时延、利用率和各区峰值，不用系统峰值替代区域峰值；对不同可行预算展示少量代表解。
- [ ] 在小实例全候选联合MILP上验证启发式差距，在代表实例扩充候选和30分钟起点网格评估敏感性。

**验收：** 联合解全部硬约束成立，执行2406无AI；对照使用相同任务、功率映射、外送边界、SOC条件和评价范围；没有用候选局部最优宣称全局最优。运行 `python -m src.cli q4 --config configs/base.json`。

## 12. T09：场景、对照、科研图和P2

### 12.1 场景定义

**文件：** `scenarios.py`、`configs/scenarios.json`、`tests/test_scenarios.py`。

- [ ] 原始场景 `R100_P1` 必须逐值复制输入，哈希关联原文件。
- [ ] 新能源规模先粗扫ρ=1.0,0.9,…,0.4，各区各时段原出力乘ρ；不改其他输入。只在迁移结构或储能价值变化超过求解不确定性的位置按0.02细扫，记录“场景转折区间”。原R75可保留补充对照，不预写任何转折位置。
- [ ] 连续低出力场景：在预先固定的1200—1223小时令新能源乘0.50，其余保持原值。它是24小时压力测试，不是真实天气概率。
- [ ] 电价平价场景：各区购电价改为该区全结算时域均值；售电价保持原始对应售购比例乘新购电价。
- [ ] 峰谷差扩大场景：每区 `新价=原均值+1.5×(原价−原均值)`，检查非负；售电同比例调整，保持售电不高于购电。若产生负价则该参数场景无效，不静默截断破坏均值控制。
- [ ] 先分别改变一种因素，再选一个 `R50＋扩大峰谷差` 联合压力场景；不直接跑庞大笛卡尔积。
- [ ] 碳预算使用同一场景“最小碳可行点Emin”与“最低成本可行点对应碳Ecost”之间的5点网格；若端点只是启发式所得，要标记为探索范围而非理论极值。端点相同或为0则如实报告退化。
- [ ] 每个核心场景比较固定任务无储能、仅任务调度、仅储能、联合。固定任务以一个全量检查通过的共同调度为基础，不直接使用可能超GPU的来源即运行基准。
- [ ] 场景中若共同固定调度在新增电网/能源条件下不可行，报告首个失效场景、违反位置和“共同基线失效区间”；不要悄悄更换该政策的任务排布。若继续研究更低新能源区间，可另定义并公开“场景特定共同可行基线”，作为扩展分析与原对照分开，禁止把两组结果连成同一交互曲线。
- [ ] 3种子只用于随机候选/随机邻域的稳定性；报告中位数、范围及最好解，不只挑一个种子。

所有情景独立使用全未来数据求解时，结论叫“离线场景比较”，不称在线控制或鲁棒策略。本合同不启用第二模型体系；未来另立版本时才讨论随机/鲁棒扩展，并重新验收。

### 12.2 图表任务

必须加载技能图表入口，使用 `export_figure.py` 导出、`plot_style.py`统一风格。每图先写“结论、源表、类别、问题编号、单位、最终尺寸”。四问×三类至少 **12张逻辑候选图**，不能把“至少9张”误当本题完整覆盖。

| 问题 | 原始数据图 raw | 过程图 process | 结果图 result |
|---|---|---|---|
| Q1 | 区域—类型GPU工作量分布 | 训练内滚动回测误差/模型选择 | 最后24小时甘特及收尾；另给利用率和预测对照 |
| Q2 | 电价—碳强度区域散点分布 | 候选扩充/局部求解实际目标轨迹 | 成本—碳可行点与迁移去向 |
| Q3 | 固定设施负荷与新能源余量时间线 | SOC及充放电过程 | 无储能/有储能的成本、碳、分区峰值变化 |
| Q4 | 假设场景输入对照，标题注明“场景输入” | 联合邻域优化真实改进轨迹 | 联合增量、等待权衡、场景转折区间 |

若碳目标退化，Q2结果图改为有实际差异的成本—等待或利用率，不画假Pareto曲线；图注解释原因。过程日志没有迭代就改画真实约束紧张度/决策变化，不捏造收敛曲线。统计、关系、时间、分布图合理搭配，至少3种图型；不同问题可复用数据，但不能改文件名凑图数。

- [ ] 每图输出SVG、≥300DPI PNG、灰度预览；颜色语义跨图一致。
- [ ] 实际打开彩色和灰度图，检查中文、符号、遮挡、坐标单位、甘特图密度；修改源代码重画，不修饰位图掩盖问题。
- [ ] 选至少8张有独立证据价值的图进入论文，四问全部覆盖；官方规则或用户有明确冲突才记录理由调整。

### 12.3 复现与P2

- [ ] 实现 `reproduce` 从只读原始输入、锁定配置和种子重建数据→四问→场景→图表→全部检查；检查点缓存只有哈希匹配可复用，独立复验至少一次禁用计算缓存。
- [ ] `validate` 核对每一个论文候选RunID，而非仅检查最好一个；任何失败退出非零。
- [ ] 使用技能 `repro_manifest.py` 生成 `results/复现清单.json`，包含6个输入、种子、配置、唯一命令、实际依赖版本和运行时间记录。
- [ ] 冻结代码、结果和图，执行确定性审计，再派发独立P2。复现数值可按公开容差比较；随机MILP的完全相同路径并非必要，但最终可行性和报告数值来源必须确定。

未来命令：

```bash
python -m src.cli scenarios --config configs/base.json
python -m src.cli figures --config configs/base.json
python -m src.cli validate --config configs/base.json
python /Users/silver/.codex/skills/math-modeling-skill/tools/figure/scripts/check_figure.py /Users/silver/math-26华数C/figures --strict
python /Users/silver/.codex/skills/math-modeling-skill/references/roles/编程手/scripts/figure_audit.py /Users/silver/math-26华数C/figures --questions q1 q2 q3 q4 --strict
python -m src.cli reproduce --config configs/base.json
```

`repro_manifest.py`的已核验参数为 `--project-root --input（可重复） --seed --parameters（JSON对象） --command --package（可重复） --runtime --output`；首次生成不要使用 `--overwrite`，后续更新在保留快照后使用。参数值从锁定配置读取，避免复制手工参数与真实运行不一致。

## 13. T10：论文证据与W1

**输入：** P2通过的模型、代码、结果、图和文献。**内部文件：** `work/论文证据大纲.md`、`work/引用核验表.csv`。不向用户交付这些内部表代替完整论文。

- [ ] 建立“主张ID→问题→结果文件/RunID/行列→公式→图→文献→章节”的映射。摘要拟引用的每个数字也必须映射。
- [ ] 先定章节与论证次序，再写长篇正文；遵循实际华数杯规则，不套国赛固定页数。
- [ ] 将“数据事实”“求解结果”“机制解释”“假设场景推论”分开标注。数据新能源同序列属于事实；储能必然无收益不是已知事实。
- [ ] 参考文献正文与文末双向对应，作者、年、题名、DOI/出版页核验；IEEE反机器人受限的候选只有交叉元数据，取得原始证据前不引用其具体结果。
- [ ] 对每问列出可写的真实结论和未解决局限；结果不足则退回对应计算任务，不预写结果再找数据。
- [ ] 派发独立W1，通过后开始T11长篇正文。

建议论文结构（官方模板有规定时以官方为准）：

| 部分 | 必须回答的内容 | 主要证据 |
|---|---|---|
| 摘要，最后写 | 四问采用什么、真实改善多少、边界是什么 | 锁定结果汇总，禁止未运行数字 |
| 问题分析与数据审计 | 为什么训练是调节重点，新能源结构为何影响路线 | T01统计与输入来源 |
| 假设与符号 | 确定性输入、时间离散、功率/碳口径 | 经M1合同与术语 |
| Q1 | 需求结构、预测切分与误差、调度可行性及甘特 | T04/T05 |
| Q2 | 边际任务成本、模型、求解规模、成本碳权衡 | T06、可行基线 |
| Q3 | 固定负荷、SOC、无储能/有储能增量、峰值波动 | T07 |
| Q4 | 联合模型、服务质量、场景比较与协同增量 | T08/T09 |
| 验证与敏感性 | 独立约束残差、小实例最优差距、候选/网格/种子 | 检查器输出与实验记录 |
| 评价与结论 | 数据和算法局限、适用条件、可推广范围 | 有证据的发现，不夸大创新 |
| 参考文献与附录 | 已核验引用、必要算法/参数、官方要求的材料 | 代码和来源 |

模型章节统一按“变量/目标/约束→算法与参数→结果→解释→验证”写，避免每问只有算法名字和结果图。篇幅按证据分配，Q4保留足够篇幅；不为了凑字数复制通用算法介绍。

## 14. T11：论文撰写、导出和W2

**文件：** `src/paper.py`、`完整论文.docx`，内部渲染存 `work/rendered/`。**依赖：** W1。

- [ ] 使用同一份正文来源和锁定结果填充Word，摘要最后生成；不要在Word里手工改数字导致与代码脱节。
- [ ] 使用技能DOCX脚本/模板构建，公式为可编辑OMML、表格为原生表格；不把公式截成图片。
- [ ] 以已核验华数杯模板决定摘要、页型、页边距、字体、编号、匿名和附录。至少8张正式图是技能质量目标，不是宣称官方最低要求。
- [ ] 特别处理校验器适用性：当前 `paper_format.py validate --contest` 仅支持 `cumcm` 和 `mcm-icm`，**没有huashu**。不能传入不存在的值，不能以cumcm通过声称华数杯格式通过。继续运行通用OOXML、公式、图表和实际渲染检查；如需华数杯规则校验，在项目内增加针对已核验规则的检查，记录技能适用性差异，不改技能根目录、不静默跳过质量检查。
- [ ] 运行DOCX环境、自检、OOXML结构校验及当前构建方式适用的门禁；若由技能LaTeX转换生成，执行 `verify-conversion`，直接构建Word则不伪造转换清单。
- [ ] 实际渲染Word，记录页数；逐页检查公式、表格分页、图注、中文字体与留白。提取正文检查Markdown符号、占位标题、示例参考文献及漏掉的第四问。
- [ ] 正文不能出现M1/P1/P2/W1/W2、Subagent回执、哈希、Checkpoint和内部执行记录；必要的科学复现参数可在方法/附录表达，内部管理术语不进入论文。
- [ ] 首个完整且通过已知格式检查的版本另存 `work/checkpoints/V1/`，同时保留绑定代码/结果配置；后续修订先保留V1，再生成V2。
- [ ] 最终冻结后派发独立W2，核对四问、数值一致、引用、图表和实际渲染；PASS才称论文完成。

默认Word的确定性命令：

```bash
python -m src.cli paper --config configs/base.json
python /Users/silver/.codex/skills/math-modeling-skill/tools/docx/scripts/office/validate.py /Users/silver/math-26华数C/完整论文.docx
```

实际DOCX渲染器由执行环境发现并按对应工具使用，先测试可用性；缺少渲染器不能仅凭文件存在称版式验收通过。

### 用户明确启用LaTeX后的分支

读取 `tools/latex/SKILL.md`，通过现有 `latex_paper.py init` 初始化项目，先 `doctor`；执行前查看当前脚本 `--help` 确认接口，不自行手写替代编译/哈希校验流程。现有2026模板需复制并适配华数杯，保留输入只读、补第四问。

同一正文和结果生成 `完整论文-LaTeX/`、`完整论文.pdf` 和 `完整论文.docx`；实际编译、绑定源资源和PDF哈希、清除引用警告并审查字体/页面/DPI。Word由LaTeX转换时遵循DOCX技能转换与 `.conversion.json` 验证。任何一个要求格式失败都不能宣称双格式完成。

## 15. T12：最终交付与完成判据

- [ ] 经M1确认的两个模型Markdown，来源/假设/选择明确。
- [ ] 可运行Python、锁定依赖、基础配置和场景配置。
- [ ] Q1：统计、完整预测与误差、538目标任务及全量前序上下文、甘特和利用率。
- [ ] Q2：实际任务碳感知方案、公平基线、四指标比较和求解局限。
- [ ] Q3：固定负荷储能策略、SOC、成本/碳/分区峰值/波动增量。
- [ ] Q4：联合方案、服务质量、碳/电价/新能源场景对照和协同增量。
- [ ] 每方案通过独立检查；无法求得的场景明确标记不可行或未求得可行解，两者不混用。
- [ ] ≥12张三类候选图，覆盖q1—q4；论文≥8张正式图或有明确规则偏离来源。
- [ ] 唯一复现命令、输入哈希、版本、种子、参数、运行时完整；独立P2 PASS。
- [ ] 完整Word参考稿实际打开并渲染，W1/W2 PASS；LaTeX只在明确要求时加入。
- [ ] 最终回复列出交付路径、实际主要指标、命令退出码、M1/P1/P2/W1/W2状态和未解决限制。不能只说“完成了”。

### 失败回退表

| 失败证据 | 返回任务 | 必须采取的处理 |
|---|---|---|
| 行数/哈希不符 | T00/T01 | 查版本与表头，不篡改预期 |
| 模型遗漏约束/量纲不一致 | T02 | 更新合同和术语，M1复验 |
| 手算/小实例不一致 | T03 | 修公共模块；禁止开始全量 |
| 预测验证不优于均值 | T04 | 保留简单模型，如实报告 |
| 候选集内不可行 | T05/T06/T08 | 加回初解、扩大候选/邻域；不声称原问题不可行 |
| 储能终端不足/互斥失败 | T07/T08 | 修SOC边界或MILP约束，重验 |
| 零碳/同一Pareto点 | T06/T09 | 报告退化，转向有意义的等待/消纳/压力场景 |
| 图表与CSV不一致 | T09 | 修改绘图源、重导出、P2复验 |
| 正文数字缺证据 | T10 | 回对应求解任务，不编数字 |
| DOCX渲染/公式/引用失败 | T11 | 修源稿，重导出，W2复验 |

## 16. 可直接发给执行 agent 的启动消息

> 请在 `/Users/silver/math-26华数C` 按 `任务执行指南.md` 执行2026华数杯C题全流程。先读指南、题目分析报告、术语表格、使用指南和原始题目附件，从T00开始逐项推进。当前已冻结模型合同v1.0，尚未实现；先核对work/M1验收记录.md是否覆盖当前文件快照，未覆盖则复验M1。默认Python、Word，原始附件和技能目录只读。尤其注意预测不替代实际任务、Q3固定附件负荷、2406无AI占用但需结算、六区域新能源序列相同、基准SOC末态不一定合规。不要伪造收益或最优性，逐步完成数据、四问求解、场景、图表、复现和论文。独立质检仅按M1/P1/P2/W1/W2节点运行，失败按证据返工。用 `work/执行记录.md` 记录实际命令与状态，在已明确的范围内持续推进，不逐步询问是否继续。没有用户明确LaTeX要求时不因存在模板目录而默认双格式。

## 17. 本指南自身的交付检查记录

本轮只新增任务指南，未创建上述实现模块，未修改题目、附件、已有两个模型讨论稿或LaTeX模板。

指南编写时实际读取了writing-plans、编程手、论文手、科研图表入口、编程质检清单和论文章节模板，并复核了本地LaTeX主文件和模型章节入口。`repro_manifest.py --help` 与 `paper_format.py validate --help` 用于确认接口；后者确认当前不支持huashu。文件搜索无匹配时的退出1仅表示未找到匹配文件，不代表模型或求解失败。

本指南通过覆盖、路径、阶段依赖及占位内容的作者检查后交付；这不新增或替代数学模型M1验收，也不表示P1/P2/W1/W2已执行。后续执行状态以第1节的实际记录为准。

## 18. 参考方案吸收后的增强任务（纳入原任务链，不另起项目）

本节与分析报告§11—15配套；以下内容是新的执行要求。保留原T00—T12顺序：E01进入T01/T02，E02进入T03/P1及T06，E03进入T06/T08，E04进入T07/T09，E05进入T08/T09，E06仅作低优先级可选实验。新增方法仍属于原数学规划模型族的求解/验证，不增加独立模型数。

### E01：数据瓶颈与外部结果隔离

- [ ] 记录两份参考文件的绝对路径、SHA-256及分析报告§11取舍表；参考中的“已跑通”不能勾选本项目T07。
- [ ] 新增直接新能源反例：RegionA第0小时无储能直供499.149MW、弃电41.041MW、购电0符合能源约束，不得加 `UsedRenewable_MW` 上限。
- [ ] 为能源分流增加手算反例：R=100、L=60、P_sell=20、弃电20、购电/充放0；应PASS。不能使用参考PDF把直供U和可用总量R混淆的平衡式。
- [ ] 复算有效IT余量 `min(Max_IT,Max_Facility/PUE)−NonAI`、逐区域电价24小时差异；“总工作量/2400÷GPU总容量约40.19%”只叫工作量平均负荷率。
- [ ] 输出 `results/data_audit.json` 中的 `reference_checks`，包括上述值、原始数据证据和不采用的假设。
- [ ] RT地区选择保留可达区域，除非明确是固定区域基线；正式模型不得把实时地区冻结当题目条件。

**验收：** 独立审阅者能从原附件解释为何拒绝直用上限、2%—3%硬超容及参考实证数字；未因参考叙事改变原始输入。

### E02：可验证下界、可行恢复和精度证书

**实现文件：** `src/relaxation.py`，测试并入 `tests/test_small_optimum.py` 与 `tests/test_checks.py`。**结果文件：** 各Q2/Q4 RunID下 `bounds.json, relaxed_flows.csv, recovery_log.csv`。

建议接口：

```python
def solve_relaxation(inputs, config, storage=False) -> dict: ...
def verify_relaxation_mapping(inputs, schedule, hourly, relaxed_config) -> dict: ...
def compare_bounds(lower_certificate, feasible_metrics) -> dict: ...
```

- [ ] 先在小实例将完整候选x及储能模式变量连续松弛，保持目标/参数；枚举原可行解，证明映射后满足松弛且成本相同。
- [ ] 全量若采用紧凑流松弛，严格按分析报告§12.1保留来源、类型、累计到达工作量和期末结清。为任务计权时延保留y_ir和工作量—指派链接；不要拿GPU·h时延替代任务平均时延。
- [ ] 实时任务采用到达固定占用曲线×地区份额，同份额贯穿任务全执行区间。所有地区固定时界标明 `conditional_on_fixed_realtime_regions`，不叫全问题下界。
- [ ] 等待预算无法在线性流中准确表达时，记录 `relaxed_constraints` 明确删除该约束，说明界更弱；不能用另一个指标冒名保留。
- [ ] 目标曲线只用于候选排序，最终落位须完整TaskID。冲突时允许偏离流曲线，冻结硬约束不变，扩大候选或邻域并重求能源。
- [ ] `bounds.json`记录目标/场景/预算/输入哈希、候选范围、松弛清单、LB来源、UB检查证书、绝对gap与归一化定义。
- [ ] LP未证最优时，只接受有效的对偶可行界作为LB；受限候选松弛的最优值仅是受限问题下界。
- [ ] 有效性单元测试：原模型中只允许时刻1到达的任务在松弛0时不能执行；A→F的80ms任务不能进入F；完整原可行方案映射后必须可行；负成本LB=-110、UB=-100时绝对gap=10元。
- [ ] 代表24/72小时实例标明独立短问题或条件子问题的边界。缩短任务截止后所得最优值不作为全题最优值。

**验收：** 每个用到的界都可说明其对应问题，原可行解映射测试通过，LB≤有效UB；没有保证gap<3%—5%的预写结果。E02小实例纳入P1、全量证据纳入P2。

### E03：价格引导与资源价值诊断

**文件：** `src/sensitivity.py`；输出 `marginal_prices.csv, carbon_budget_sensitivity.csv, bottlenecks.csv`。

- [ ] 从正确能源模型的连续松弛读取λ和μ，记录对偶对应的约束方向、目标、预算和松弛清单；用小幅需求/碳预算扰动核验符号及单位。
- [ ] 候选能源评分用 `GPU_Demand×p_type×PUE×Σ(Overlap×λ)`。λ已经来自含碳约束模型时不再加μCI；评分只作为启发式，不写成真实增量成本公式。
- [ ] MILP价格分析用同一问题参数有限差分，不直接请求全局Pi；固定整数LP对偶明确仅为条件信息。
- [ ] 原场景碳目标失活时允许零μ，不能通过直用上限或人为购电构造非零碳价。
- [ ] 统计最终方案各项容量余量与绑定频率，再做小幅资源放宽；IT和设施等价上限需要同步放宽，不能把冗余约束分别当两个瓶颈。
- [ ] 优化前的λ不能依赖Q1测试未来实际任务；Q2/Q4离线分析可以使用正式实际输入，但不能回流用于宣称Q1预测无泄漏。

**验收：** 价格来源/单位/符号明确、差分与对偶在适用范围内一致；不同模型的价格不直接混用。无适用对偶时给有限差分和误差范围，不伪造价格。

### E04：储能电量、容量与外送通道的价值分解

**文件：** `src/sensitivity.py`；输出 `storage_energy_value.csv, storage_capacity_value.csv, export_capacity_value.csv`。

- [ ] 严格使用分析报告§14.2的三种V_cost差分定义；先做原场景及R50压力场景，不默认扫描所有2407时刻。
- [ ] 存量电量价值在选定的代表时刻固定其前序状态、未来负荷和原终端要求，仅改变时段开始SOC；δ=0.1/1/5MWh中只运行边界可行点，不把多给SOC视为正式免费能源。
- [ ] 容量取基准1.0/1.1/1.25/1.5倍，保持同一初始电量、MinSOC、终端下限和充放功率。若另做功率扩容，单列场景，不与容量混合。
- [ ] DEF外送通道按小幅增加扫描，MaxGridExport和SellLimit同步更新；ABC开放外送只作额外假设政策情景。
- [ ] 每个差分记录两次求解状态/界/残差和期长；目标变化低于求解不确定度时不宣称精确边际平台。
- [ ] Q3固定负荷价值不能直接写入Q4目标作为已正确的价值函数；Q4解附近重估，或仅将Q3结果作为对照。

**验收：** 输出单位分别是元/MWh电量、元/MWh容量/结算期、元/MW通道/结算期；初态与终端没有随容量比例变化。真实最优值的资源放宽单调性通过，启发式不单调时先保留原可行解并复算。

### E05：四格对照检验协同和替代

- [ ] 使用T09共同可行基线，形成F00/F10/F01/F11，成本之外同时报告同一套碳、等待、利用率和峰值指标。
- [ ] 输出 `interaction.csv`：联合总收益、任务调度后的储能增量、储能优化后的任务增量，以及 `F10+F01−F11−F00`。
- [ ] 用各政策有效界计算I_low=L10+L01−U11−U00、I_high=U10+U01−L11−L00。仅区间整体高于预设τ_I或低于−τ_I才解释互补/替代；否则注明当前精度不能判断符号。缺少有效界时仅报已实现方案交互量，不认证最优政策效应。任何对照不可行或缺失则不计算交互量。
- [ ] 新灵活性允许不用，政策初解嵌套；正式比较保留同一硬约束与预算，不把因改服务要求得到的收益归因于协同。
- [ ] 增加至少一张“任务积压与物理SOC”对照图，分别使用GPU·h和MWh，采用独立子图与清晰单位；不能把两者画成可直接相加的能量库存。

**验收：** 四格RunID可追溯，交互量公式与分析报告相同；结论可为互补或替代，不预设联合收益超加性。

### E06：可选的预测经济意义诊断

- [ ] 若主线已完整，按分析报告§15增加测试前冻结价格加权的到达工作量误差，保留正式18×24预测及原精度指标。
- [ ] 命名为“价格加权误差诊断”，不叫实际调度成本增加；禁止将预测工作量替换实际TaskID进入正式调度。
- [ ] 若要研究真实预测后悔值，另写信息结构、预留动作和到达后修复合同，经过模型审阅后再做；默认不开展该扩展。

### 修订后的论文主线与门禁

论文可组织为“任务时空弹性与物理储能协同”，重点证据从算法数量改为：功率瓶颈→有效上下界→条件边际价值→四格交互与场景转折区间。参考材料的“虚拟储能完全等价”“每问全局最优”“储能省22%”“MAPE个位数”和28—34张图/国赛30页，不作为本项目预设结论或格式要求。

本轮已冻结模型合同v1.0并重新派发独立M1；回执见work/M1验收记录.md，没有覆盖当前文件快照的PASS前不得进入正式实现。旧稿因用量限制中断的回执不覆盖本版。E01—E05覆盖情况分别纳入P1/P2/W1/W2检查，不增加实现子agent或另造门禁名。
